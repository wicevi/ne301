# NE301 HTTP API 参考文档

| 项目 | 内容 |
|---|---|
| **适用固件版本(APP)** | **v4.3.1.61** |
| 固件构建时间 | 2026-09-03 10:13:26 |
| Git 提交 | `e42ea18-dirty`(分支 `main`) |
| 文档生成日期 | 2026-09-03 |
| 文档来源 | 依据上述工作区代码 `Custom/Services/Web/` 自动整理 |

> ⚠️ 本文档对应 `e42ea18` 工作区(含未提交修改)。若固件与本文描述不一致,以固件实现为准。

## 0. 软件版本信息

| 组件 | 版本 | 说明 |
|---|---|---|
| APP(主应用固件) | **4.3.1.61** | `Custom/Common/Inc/version.h` 自动生成 |
| FSBL(一级引导) | 1.0.3.1(期望值) | `EXPECTED_FSBL_VERSION_STRING`,实际版本运行时比对 |
| WiFi/Si91x 固件 | 2.16.5.108(期望值) | `EXPECTED_WIFI_VERSION_STRING`,实际版本运行时比对 |
| Web 资源(前端) | 1.5.0.6 | 随版本包发布,运行时经 `/device/firmware-versions` 查询 |
| AI 模型包 | 4.0.0.0 | 同上 |
| WakeCore | 0.2.7.5 | 同上 |

运行时通过 `GET /api/v1/device/firmware-versions` 可查询各组件**实际运行版本**,通过 `GET /api/v1/device/version-check` 可比对 FSBL/WiFi 固件是否与 APP 期望一致。

---

## 1. 通用约定

### 1.1 服务与基础地址

| 项目 | 值 |
|---|---|
| HTTP 服务 | Mongoose,监听 **TCP 80** 端口 |
| HTTPS | 未启用(`IS_HTTPS=0`) |
| API 基础路径 | **`/api/v1`** |
| 单请求体上限 | 5 MB(经路由器的普通请求) |
| 连接模式 | `Connection: close`,一连接一请求 |
| 静态资源 | Web UI 由根路径 `/` 直接提供(支持 ETag/304/gzip) |

设备地址:
- STA/以太网模式:设备获得的 IP(见路由器/AP,或经配置工具查询)
- AP 模式:设备热点网关地址(默认 `10.10.10.1` 网段,以设备实际配置为准)

### 1.2 认证

- 采用 **HTTP Basic 认证**,每个请求独立校验,无会话/Cookie。
- 用户名固定 **`admin`**(`auth_mgr.h: AUTH_ADMIN_USERNAME`),密码为设备访问密码。
- 密码可经 `POST /api/v1/login` 校验、经 `POST /api/v1/change-password` 修改。

**免认证端点**(路由表中 `require_auth=false`):

| 端点 | 说明 |
|---|---|
| `POST /api/v1/login` | 密码校验 |
| `GET /api/v1/preview/status` | 预览状态 |
| `POST /api/v1/preview/start` | 启动预览 |
| `POST /api/v1/preview/stop` | 停止预览 |

> 注:`POST /api/v1/system/ota/upload` 在路由分发之前被流式拦截器接管,自行校验,详见 16.2 节。

**认证失败响应**(普通接口):HTTP 状态码仍为 **200**,信封内 `error_code: "UNAUTHORIZED"`。流式上传接口(`files/upload`、`ota/upload`)例外,返回真实 `401`。

### 1.3 统一响应信封

所有经路由器的接口,无论成功失败,**HTTP 状态码恒为 200**,正文为 JSON 信封:

```jsonc
// 成功
{
  "success": true,
  "message": "Device information retrieved successfully",   // 可选
  "data": { ... }                                            // 可选
}

// 失败
{
  "success": false,
  "error_code": "INVALID_PASSWORD",   // 仅失败时出现,字符串名
  "message": "Invalid password"
}
```

| 字段 | 类型 | 说明 |
|---|---|---|
| success | bool | 业务成败 |
| error_code | string | 仅失败时出现,大写下划线字符串(见 1.5) |
| message | string | 人读描述,成功/失败均可能出现 |
| data | object/array/null | 业务数据,可能缺省 |

> **客户端务必以 `success` 字段判错,不能依赖 HTTP 状态码。**

### 1.4 信封例外(返回真实 HTTP 状态码/非 JSON 的接口)

| 接口 | 响应形式 |
|---|---|
| `GET /api/v1/files/download` | 原始文件字节流 + `Content-Disposition`(404/400 为信封 JSON) |
| `GET /api/v1/files/preview` | 图片原始字节;文本文件为 JSON(content 为字符串) |
| `POST /api/v1/files/upload` | 流式直写,认证失败 **401**、参数错 400、内部错 500/503(真实状态码) |
| `POST /api/v1/system/ota/upload` | 流式直写,真实状态码 400/401/500 |
| `POST /api/v1/system/ota/export` | 原始固件字节流 + `Content-Disposition` |
| `GET /` 静态资源 | 200/304/404 |
| `OPTIONS` 预检 | 200 + CORS 头 |

### 1.5 错误码

#### 1.5.1 框架错误码(web_server.h `api_error_code_t`)

| HTTP 语义 | error_code 字符串 | 触发场景 |
|---|---|---|
| 400 | `INVALID_REQUEST` | 参数缺失/非法、JSON 解析失败、Content-Type 错 |
| 401 | `UNAUTHORIZED` | Basic 认证失败 |
| 404 | `NOT_FOUND` | 路径未注册 |
| 405 | `METHOD_NOT_ALLOWED` | 方法不匹配(方法匹配不区分大小写) |
| 500 | `INTERNAL_ERROR` | 内部错误 |
| 503 | `SERVICE_UNAVAILABLE` | 依赖服务未运行 |

#### 1.5.2 业务错误码(api_business_error.h,1001 起)

| 码 | error_code 字符串 | 含义 |
|---|---|---|
| 1001 | `INVALID_PASSWORD` | 密码错误 |
| 1002 | `INVALID_CREDENTIALS` | 用户名或密码错误 |
| 1003 | `UNAUTHORIZED` | 未授权 |
| 1004 | `SESSION_EXPIRED` | 会话过期 |
| 1005 | `TOKEN_INVALID` | 令牌无效 |
| 1007 | `PERMISSION_DENIED` | 权限不足 |
| 1101 | `INVALID_PARAM` | 参数非法 |
| 1102 | `MISSING_PARAM` | 缺少必填参数 |
| 1103 | `PARAM_OUT_OF_RANGE` | 参数超范围 |
| 1104 | `INVALID_FORMAT` | 数据格式错误 |
| 1201 | `DEVICE_OFFLINE` | 设备离线 |
| 1202 | `DEVICE_BUSY` | 设备忙 |
| 1204 | `CAMERA_ERROR` | 摄像头错误 |
| 1206 | `HARDWARE_ERROR` | 硬件错误 |
| 1301 | `NETWORK_ERROR` | 网络错误 |
| 1302 | `NETWORK_TIMEOUT` | 网络超时 |
| 1304 | `MQTT_NOT_CONNECTED` | MQTT 未连接 |
| 1305 | `WIFI_NOT_CONNECTED` | WiFi 未连接 |
| 1401 | `STORAGE_FULL` | 存储已满 |
| 1403 | `FILE_NOT_FOUND` | 文件不存在 |
| 1407 | `INSUFFICIENT_SPACE` | 存储空间不足 |
| 1408 | `DIR_NOT_EMPTY` | 目录非空(不能删除) |
| 1501 | `MODEL_NOT_LOADED` | AI 模型未加载 |
| 1502 | `MODEL_INVALID` | AI 模型无效 |
| 1503 | `MODEL_RELOAD_FAILED` | 模型重载失败 |
| 1505 | `INFERENCE_TIMEOUT` | 推理超时 |
| 1601 | `CONFIG_INVALID` | 配置非法 |
| 1602 | `CONFIG_NOT_FOUND` | 配置不存在 |
| 1604 | `CONFIG_UPDATE_FAILED` | 配置更新失败 |
| 1701 | `OPERATION_TIMEOUT` | 操作超时 |
| 1702 | `OPERATION_FAILED` | 操作失败 |
| 1705 | `OPERATION_IN_PROGRESS` | 操作进行中(忙) |
| 1706 | `AT_COMMAND_FAILED` | AT 命令失败(4G 模组) |
| 1801 | `FIRMWARE_INVALID` | 固件非法 |
| 1804 | `OTA_IN_PROGRESS` | OTA 升级进行中 |
| 1805 | `OTA_FAILED` | OTA 升级失败 |
| 1806 | `OTA_HEADER_VALIDATION_FAILED` | OTA/Bundle 头校验失败 |
| 1901 | `RESOURCE_NOT_FOUND` | 资源不存在 |
| 1902 | `RESOURCE_BUSY` | 资源忙 |
| 2001–2008 | `ISP_*`(见第 18 章) | ISP 业务错误(2001 未初始化/2002 参数非法/2003 超范围/2004 依赖错误/2005 HAL 错误/2006 传感器错误/2007 算法错误/2008 忙) |
| 9999 | `UNKNOWN` | 未知错误 |

### 1.6 CORS 与预检

所有响应携带 CORS 头(`Access-Control-Allow-Origin: *` 等);`OPTIONS` 预检请求直接返回 200 + CORS 头,不进入业务路由。

### 1.7 实时预览流(WebSocket,非 HTTP API)

预览流走独立 WebSocket 服务:**`ws://<设备IP>:8081/stream`**(默认端口 8081、路径 `/stream`)。详见[附录 B](#附录-b-websocket-预览流)。

---

## 2. 接口总览

共 15 个模块、约 150 个端点,全部挂载于 `/api/v1` 下。除标注"免认证"外均需 Basic 认证。

| # | 模块 | 前缀 | 端点数 |
|---|---|---|---|
| 3 | 认证 | `/login`、`/change-password` | 2 |
| 4 | 设备与系统 | `/device/*`、`/system/*` | 24 |
| 5 | 网络 | `/system/network/*`、`/wifi/*`、`/halow/*`、`/comm/*`、`/cellular/*`、`/poe/*` | 47 |
| 6 | 抓拍与上传 | `/capture/*` | 7 |
| 7 | 实时预览 | `/preview/*`(免认证) | 3 |
| 8 | 文件管理 | `/files/*` | 8 |
| 9 | AI 检测 | `/ai/*` | 6 |
| 10 | 模型管理 | `/model/*` | 2 |
| 11 | 工作模式/电源模式 | `/work-mode/*`、`/power-mode/*` | 9 |
| 12 | MQTT | `/apps/mqtt/*`、`/device/capture` | 8 |
| 13 | RTMP 推流 | `/apps/rtmp/*` | 5 |
| 14 | RTSP 服务 | `/apps/rtsp/*` | 5 |
| 15 | Webhook | `/apps/webhook/*` | 6 |
| 16 | 固件升级 OTA | `/system/ota/*` | 7 |
| 17 | ISP 图像质量 | `/isp/*` | 27 |
| — | 静态资源/Web UI | `/` | — |

> 网络 HaLow 相关 10 个端点仅在固件编译启用 HaLow(`NETIF_WIFI_HALOW_IS_ENABLE`)时注册,当前构建已启用。

---

## 3. 认证模块(auth)

### 3.1 POST /api/v1/login — 校验设备密码

**免认证。** 用于前端登录页校验密码(用户名固定 `admin`,不随请求传递)。

**请求体**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| password | string | 是 | 设备访问密码 |

**响应 data**:`null`(成功时仅有 `success:true` + message)

**错误**:`INVALID_REQUEST`(缺参);`INVALID_PASSWORD`(1001,密码错误)

### 3.2 POST /api/v1/change-password — 修改设备密码

**请求体**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| password | string | 是 | 新密码,**长度 8–32 字符** |

**响应 data**:`null`

**说明**:修改立即生效,后续请求需用新密码做 Basic 认证。

**错误**:`INVALID_REQUEST`(缺参/长度非法);`INVALID_PASSWORD`(修改失败)

---

## 4. 设备与系统模块(device / system)

### 4.1 GET /api/v1/device/info — 设备信息

**响应 data**

| 字段 | 类型 | 说明 |
|---|---|---|
| device_name | string | 设备名称 |
| mac_address | string | MAC 地址 |
| serial_number | string | 序列号 |
| hardware_version | string | 硬件版本 |
| software_version | string | 软件版本(即 APP 固件版本) |
| camera_module | string | 摄像头模组 |
| extension_modules | string | 扩展模组 |
| power_supply_type | string | 供电类型 |
| battery_percent | number | 电量百分比 |
| communication_type | string | 当前通信类型 |
| storage_card_info | string | 存储卡信息 |
| storage_usage_percent | number | 存储使用率(%) |

### 4.2 GET /api/v1/device/firmware-versions — 各组件固件版本

**响应 data**

| 字段 | 类型 | 说明 |
|---|---|---|
| fsbl | string | FSBL 实际运行版本 |
| app | string | APP 实际运行版本 |
| web | string | Web 资源版本 |
| model | string | AI 模型包版本 |
| wakecore | string | WakeCore 版本 |
| wifi | string | Si91x WiFi/HaLow 固件版本 |
| wifi_running | string | WiFi 固件运行态标识 |
| expected_fsbl | string | APP 期望的 FSBL 版本 |
| expected_wifi | string | APP 期望的 WiFi 固件版本 |

### 4.3 GET /api/v1/device/version-check — FSBL/WiFi 版本一致性检查

**响应 data**

| 字段 | 类型 | 说明 |
|---|---|---|
| fsbl_mismatch | bool | FSBL 实际版本 ≠ 期望版本 |
| wifi_mismatch | bool | WiFi 固件实际版本 ≠ 期望版本 |
| expected_fsbl | string | 期望 FSBL 版本 |
| current_fsbl | string | 实际 FSBL 版本 |
| expected_wifi | string | 期望 WiFi 固件版本 |
| current_wifi | string | 实际 WiFi 固件版本 |

### 4.4 GET /api/v1/device/storage — 存储信息

**响应 data**(SD 卡 + flash 双分区)

| 字段 | 类型 | 说明 |
|---|---|---|
| sd_card_connected | bool | SD 卡是否在位 |
| total_capacity_mb / available_capacity_mb / used_capacity_mb | number | SD 卡容量(MB) |
| usage_percent | number | SD 卡使用率 |
| total_capacity_gb / available_capacity_gb / used_capacity_gb | number | SD 卡容量(GB) |
| cyclic_overwrite_enabled | bool | 循环覆盖开关 |
| overwrite_threshold_percent | number | 覆盖阈值(%) |
| flash_fs_mounted | bool | flash 文件系统是否挂载 |
| flash_fs_error | bool | flash 文件系统是否异常 |
| flash_error | string | flash 错误描述 |
| flash_total_capacity_mb / flash_available_capacity_mb / flash_used_capacity_mb | number | flash 容量(MB) |
| flash_usage_percent | number | flash 使用率 |
| flash_total_capacity_gb / flash_available_capacity_gb / flash_used_capacity_gb | number | flash 容量(GB) |
| flash_fs_type | string | flash 文件系统类型(littlefs) |
| status | string | 存储状态摘要 |
| primary_storage | string | 主存储(sd / flash) |

### 4.5 POST /api/v1/device/storage/config — 循环覆盖配置

**请求体**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| cyclic_overwrite_enabled | bool | 否 | 循环覆盖开关 |
| overwrite_threshold_percent | number | 否 | 覆盖阈值 |

**响应 data**:回显 `{cyclic_overwrite_enabled, overwrite_threshold_percent}`

### 4.6 POST /api/v1/device/storage/format — 格式化存储

**请求体**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| target | string | 否 | 格式化目标(`sd` / `flash`,默认 `sd`) |

**响应**:异步执行,返回接受结果;进度经 `/device/storage` 查询。

### 4.7 POST /api/v1/device/name — 修改设备名称

**请求体**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| device_name | string | 是 | 新设备名 |

**响应 data**:回显 `{device_name}`

### 4.8 GET / POST /api/v1/device/image/config — 图像配置

GET 返回当前配置;POST 部分更新(只传需改字段)。

**字段**(GET 返回 / POST 可选)

| 字段 | 类型 | 范围/取值 | 说明 |
|---|---|---|---|
| brightness | number | 0–100 | 亮度 |
| contrast | number | 0–100 | 对比度 |
| horizontal_flip | bool | — | 水平翻转 |
| vertical_flip | bool | — | 垂直翻转 |
| aec | number | 0–100 | 自动曝光目标 |
| isp_mode | number | 0(室外)/ 1(室内)/ 255(自定义) | ISP 场景模式 |
| grayscale | bool | — | 灰度输出 |
| fast_capture_skip_frames | number | 0–300 | 快速抓拍跳帧数 |
| fast_capture_resolution | number | 0(1280×720)/ 1(1920×1080)/ 2(2688×1520) | 快速抓拍分辨率 |
| fast_capture_jpeg_quality | number | 1–100 | 快速抓拍 JPEG 质量 |
| capture_disable_comm | bool | — | 抓拍期间暂停通信 |
| capture_storage_ai | bool | — | 抓拍图存储 AI 结果叠加 |

**说明**:POST 修改 brightness/contrast/翻转/aec 时会自动重启 AI 流水线(先停后启)。

### 4.9 GET / POST /api/v1/device/sys-clk/config — CPU 主频配置

由 FSBL 持久化,**重启后生效**。

**GET 响应 data**

| 字段 | 类型 | 说明 |
|---|---|---|
| valid | bool | 配置是否有效读出 |
| sys_clk_profile | number | 当前主频档位 |

**POST 请求体**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| sys_clk_profile | number | 是 | 1=HSE 200MHz,2=HSE 400MHz,3=HSI 800MHz,4=HSE 800MHz |

### 4.10 GET / POST /api/v1/device/light/config — 补光灯配置

**GET 响应 data**

| 字段 | 类型 | 说明 |
|---|---|---|
| connected | bool | 补光灯是否连接 |
| mode | string | `off` / `on` / `auto` / `custom` |
| brightness_level | number | 亮度 0–100 |
| fill_light_while_streaming | bool | 工作期补光模式(运行期间灯无条件按配置亮) |
| custom_schedule | object | 定时亮灯计划 `{start_hour, start_minute, end_hour, end_minute}` |

**POST 请求体**(均可选)

| 字段 | 类型 | 说明 |
|---|---|---|
| mode | string | `off`/`on`/`auto`/`custom` |
| brightness_level | number | 0–100 |
| fill_light_while_streaming | bool | 见 4.10 |
| custom_schedule | object | 同 GET |

### 4.11 GET / POST /api/v1/device/camera/config — 摄像头配置

**字段**

| 字段 | 类型 | 说明 |
|---|---|---|
| enabled | bool | 摄像头使能 |
| width / height | number | 分辨率 |
| fps | number | 帧率 |
| image_config | object | 完整图像配置,字段同 4.8 |

### 4.12 POST /api/v1/system/time — 设置系统时间

**请求体**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| utc_timestamp | number | 与 local_timestamp 二选一 | UTC 秒级时间戳 |
| local_timestamp | number | 与 utc_timestamp 二选一 | 本地秒级时间戳 |
| timezone_offset_hours | number | 否 | 时区偏移(小时) |

**响应 data**:`{message, utc_timestamp, local_timestamp, timezone_offset_hours, utc_time, local_time}`

**说明**:设备同时支持 SNTP/U0 对时;web 设置走 RTC 调度器(带步进守卫)。

### 4.13 GET /api/v1/system/logs — 读取日志

**查询参数**:`file`(日志文件名,可选,默认主日志)

**响应 data**:`{content(日志文本), size}`;文件级列表见 4.14。

### 4.14 GET /api/v1/system/logs/export — 导出日志

**响应 data**:`{log_files: [{filename, size, content}], total_files, total_size}`

### 4.15 POST /api/v1/system/restart — 重启设备

**请求体**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| delay_seconds | number | 否 | 延迟秒数(默认 3) |

**响应 data**:`{delay_seconds}`(异步执行,后台任务延迟后复位)

### 4.16 POST /api/v1/system/factory-reset — 恢复出厂

**请求体**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| delay_seconds | number | 否 | 延迟秒数 |

**响应 data**:`{restart_delay_seconds}`(异步:清配置后自动重启)

### 4.17 GET /api/v1/device/config/export — 导出设备配置

**响应 data**

| 字段 | 类型 | 说明 |
|---|---|---|
| export_version | string | 导出格式版本 |
| export_timestamp | number | 导出时间戳 |
| config | object | 结构化配置(分模块) |
| config_raw | string | 原始配置文本 |

### 4.18 POST /api/v1/device/config/import — 导入设备配置

**请求体**:4.17 导出的 `data` 原样(或仅 `config` 对象)。

**响应 data**:`{message, config_version, timestamp, checksum, saved_to_file}`

### 4.19 GET / POST /api/v1/device/preference/stream_tab — Web UI 偏好

**GET 响应 data**:`{stream_tab: string}`(前端记住的流页签)
**POST 请求体**:`{stream_tab: string}`

---

## 5. 网络模块(network)

### 5.1 GET /api/v1/system/network/status — 通信总状态

**响应 data**

| 字段 | 类型 | 说明 |
|---|---|---|
| service_state | string | 通信服务状态 |
| service_version | string | 服务版本 |
| active_type | string | 当前活跃通信类型(wifi/cellular/poe/halow…) |
| selected_type | string | 选定类型 |
| current_comm_type | string | 当前通信类型 |
| has_connection | bool | 是否有可用连接 |
| active_display_name / selected_display_name / current_comm_display_name | string | 显示名 |
| available_comm_types | array | `[{type, display_name, is_selected, is_active, is_connected, is_current}]` |
| available_comm_count | number | 可用类型数 |
| preferred_type | string | 偏好类型 |
| auto_priority | bool | 自动优先级选择 |
| is_auto_selection | bool | 是否自动选择模式 |
| selection_mode | string | 选择模式 |
| current_comm_info | object | `{status, ip_address, signal_strength, ssid?, isp?, message}` |

### 5.2 GET /api/v1/system/network/wifi/sta — WiFi STA 状态

**响应 data**:`{connected, ssid, bssid, rssi, channel, ip_address, mac_address, state}`

### 5.3 GET /api/v1/system/network/wifi/ap — WiFi AP 状态

**响应 data**:`{enabled, state, ip_address, mac_address, channel, ssid, password, ap_sleep_time, security}`

> AP 密码明文返回(设备侧无脱敏)。

### 5.4 POST /api/v1/wifi/config — 配置并连接 WiFi(别名:POST /system/network/wifi)

**请求体**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| interface | string | 否 | `ap`(配 AP 热点)/ `wl`(连 STA),默认 STA |
| ssid | string | 是 | SSID,1–31 字符 |
| password | string | 是 | 密码,8–63 字符 |
| bssid | string | 否 | 指定 BSSID |
| ap_sleep_time | number | 否 | AP 无客户端休眠时间 0–3600 秒(interface=ap 时) |
| use_saved_password | bool | 否 | 使用已存密码连接 |

**响应 data**:`{message, interface, ssid, ap_sleep_time}`(异步连接,结果轮询 5.1/5.2)

### 5.5 POST /api/v1/wifi/scan — 扫描 WiFi(别名:POST /system/network/scan)

**请求体**:空 JSON 或 `{}`(可带 `force`)

**响应 data**(异步,首次返回扫描中,稍后再查取结果)

| 字段 | 类型 | 说明 |
|---|---|---|
| scan_results | object | `{known_networks[], unknown_networks[], known_count, unknown_count}` |
| scan_in_progress | bool | 是否扫描中 |

扫描条目字段:

| 字段 | 说明 |
|---|---|
| ssid / bssid | SSID / BSSID |
| rssi | 信号强度 |
| channel | 信道 |
| security | 安全类型(open/wpa2…) |
| connected | 是否当前连接 |
| is_known / quick_connect | 是否已存 / 可秒连 |
| last_connected_time | 上次连接时间戳 |

### 5.6 POST /api/v1/wifi/disconnect — 断开 WiFi(别名:POST /system/network/disconnect)

**请求体**:`{interface: "wl"}`。响应 `data:{message, interface, if_name, status}`

### 5.7 POST /api/v1/wifi/delete — 删除已存 WiFi(别名:POST /system/network/delete)

**请求体**:`{ssid}`(interface 可选)。响应 `data:{status, message}`

### 5.8 GET /api/v1/wifi/region — WiFi 地区码

**响应 data**:`{region, active_region, supported_regions[]}`(仅 us/eu/jp/kr/cn 有效)

### 5.9 PUT /api/v1/wifi/region — 设置 WiFi 地区码

**请求体**:`{region}`。**响应 data**:`{region, restart_required, message}`(部分地区码需重启 WiFi 生效)

### 5.10 HaLow 组(`/halow/*`,10 个端点)

| 端点 | 方法 | 说明 |
|---|---|---|
| /api/v1/halow/sta | GET | HaLow STA 状态:`{connected, ssid, bssid, rssi, channel, ip_address, netmask, gateway, ip_mode, mac_address, state, region, supported_regions[]}` |
| /api/v1/halow/region | GET | `{region, supported_regions[]}` |
| /api/v1/halow/region | PUT | `{region}` → `{region, message, scan_required}` |
| /api/v1/halow/scan | POST | 触发扫描,响应条目结构同 5.5 |
| /api/v1/halow | POST | 连接:`{ssid, password?, bssid?, security?, region?, use_saved_password?}` → `{message, interface, ssid, bssid}` |
| /api/v1/halow/disconnect | POST | `{message, interface, status}` |
| /api/v1/halow/delete | POST | `{ssid}` 删除已存网络 |
| /api/v1/halow/ip | GET/POST | IP 配置:`{ip_mode(dhcp/static), ip_address, netmask, gateway}` |
| /api/v1/halow/radio | GET | 射频参数(见下) |
| /api/v1/halow/radio | PUT | 射频参数(见下) |

**radio 字段**:`{tx_power_dbm, scan_dwell_ms, rate_mcs(-1..9), rate_bw_mhz(-1/1/2/4/8), rate_gi(-1..1), ps_mode(0/1), limits:{tx_power_dbm:{min,max}, scan_dwell_ms:{min,max,default}}}`

> `ps_mode` 为省电模式开关;HaLow 与 PoE 硬件互斥(EXTI15),切换由通信服务自动协调,无需手工下电另一方。

### 5.11 通信类型组(`/comm/*`,4 个端点)

| 端点 | 方法 | 说明 |
|---|---|---|
| /api/v1/comm/types | GET | 全量类型清单:`{current_type, preferred_type, auto_priority, types:[{type, status, available, connected, can_switch, is_current, ip_address, signal_strength, priority, display_name, detail:{imei?, apn?, isp?, ssid?, rssi?, channel?, interface?}}], available_types[], type_count, current_type_info:{type, status, ip_address, signal_strength, ssid?, isp?, network_type?, csq?, signal_level?}}` |
| /api/v1/comm/switch | POST | `{type}`(wifi/cellular/poe/halow)切换,**阻塞最长 30 秒** → `{success, from_type, to_type, switch_time_ms, error?}` |
| /api/v1/comm/prefer | GET/POST | 偏好类型:`{preferred_type, auto_priority}` |
| /api/v1/comm/priority | POST | 优先级调整 → `{current_type, success}` |

### 5.12 4G 蜂窝组(`/cellular/*`,9 个端点)

| 端点 | 方法 | 说明 |
|---|---|---|
| /api/v1/cellular/status | GET | `{available, status, message, imei, settings:{apn, username, password, pin_code, authentication, enable_roaming, operator, plmn}}` |
| /api/v1/cellular/info | GET | 模组详情:`{network_status, sim_status, model, version, imei, imsi, iccid, isp, network_type, register_status, plmn_id, lac, cell_id, signal_level, csq, csq_level, rssi, ipv4_address, ipv4_gateway, ipv4_dns, ipv6_address, ipv6_gateway, ipv6_dns, connection_duration_sec, error?}` |
| /api/v1/cellular/settings | GET/POST | 字段同 status.settings;POST 均可选 |
| /api/v1/cellular/save | POST | 保存设置到 NVS → `{message, apn, enable_roaming}` |
| /api/v1/cellular/connect | POST | 拨号连接 → `{success, status}` |
| /api/v1/cellular/disconnect | POST | 断开 → `{success, status}` |
| /api/v1/cellular/refresh | POST | 刷新模组状态 → `{success, status}` |
| /api/v1/cellular/at | POST | 直发 AT 命令:`{command}` → `{success, command, response}`;失败返回 `AT_COMMAND_FAILED`(1706) |

### 5.13 PoE 组(`/poe/*`,8 个端点)

| 端点 | 方法 | 说明 |
|---|---|---|
| /api/v1/poe/status | GET | `{available, status, message, ip_address, connected}` |
| /api/v1/poe/info | GET | 链路详情:`{available, network_status, status_code, status_message, ip_mode, ip_address, netmask, gateway, dns_primary, dns_secondary, hostname, mac_address, interface_name, link_up, poe_powered, connection_duration_sec, connection_start_time, dhcp_lease_time, dhcp_lease_remaining, connect_count, disconnect_count, dhcp_fail_count, last_error_code, error?}` |
| /api/v1/poe/config | GET | `{ip_mode, ip_address, netmask, gateway, dns_primary, dns_secondary, hostname, dhcp_timeout_ms, dhcp_retry_count, dhcp_retry_interval_ms, power_recovery_delay_ms, auto_reconnect, persist_last_ip, validate_gateway, detect_ip_conflict}` |
| /api/v1/poe/config | POST | 同上字段均可选 → `{message, ip_mode}` |
| /api/v1/poe/connect | POST | `{success, status}` |
| /api/v1/poe/disconnect | POST | `{success, status}` |
| /api/v1/poe/validate | POST | 校验当前网络配置:`{ip_conflict, gateway_reachable, valid, errors[], warnings[]}` |
| /api/v1/poe/apply / /api/v1/poe/save | POST | 应用/保存配置 → `{success, status, ip_mode}` |

---

## 6. 抓拍与上传模块(capture)

### 6.1 GET / POST /api/v1/capture/upload-config — 抓拍上传配置

**GET 响应 data / POST 请求体**(POST 字段均可选,部分更新)

| 字段 | 类型 | 取值 | 说明 |
|---|---|---|---|
| version | number | — | 配置版本(只读) |
| mode | string | `instant`(即拍即传)/ `batch`(攒批)/ `scheduled`(定时)/ `local_only`(仅本地) | 上传模式 |
| storage | string | `auto` / `flash` / `sd` / `none` | 抓拍存储位置 |
| policy | string | `wrap`(循环覆盖)/ `stop`(停写) | 满盘策略 |
| upload_protocol | string | `mqtt` / `webhook` | 上传通道 |
| retry_max_attempts | number | ≥0 | 上传重试次数上限 |
| batch_count | number | — | 攒批条数 |
| schedule_minutes | number[] | 0–59 | 定时分钟表(scheduled 模式) |
| keep_sent_hours | number | — | 已传记录保留时长(小时) |
| max_pending_records | number | — | 待传记录上限(SD) |
| flash_max_records | number | — | flash 记录上限 |
| upload_network | string | — | 限定上传网络 |

**约束**:`storage=none` 时必须 `mode=instant`(否则 400)。

**POST 响应 data**:`{success}` 或逐字段回显。

### 6.2 GET /api/v1/capture/queue — 上传队列状态

**响应 data**

| 字段 | 类型 | 说明 |
|---|---|---|
| mode / storage | string | 当前模式/存储 |
| actual_fs | string | 实际生效文件系统 |
| pending_count | number | 待传数 |
| sent_count | number | 已传数 |
| failed_count | number | 失败数 |
| local_count | number | 仅本地数 |
| bytes_used_kb / bytes_available_kb | number | 容量 |
| next_scheduled_at | number | 下次定时上传时间戳 |

### 6.3 GET /api/v1/capture/records — 抓拍记录查询

**查询参数**

| 参数 | 说明 |
|---|---|
| state | 过滤状态(pending/sent/failed/local) |
| offset / limit | 分页,limit ≤ 200 |
| from / to | 时间戳范围 |
| sort | 排序 |

**响应 data**:`{state, offset, limit, total, records:[{id, state, timestamp, size, retry_count, trigger, last_error}]}`

### 6.4 DELETE /api/v1/capture/records — 清空记录

**响应 data**:`{reset_count}`

### 6.5 POST /api/v1/capture/records/retry — 重试失败记录

**请求体**:`{id}`(单条)或 `{all_failed: true}`
**响应 data**:`{success, id}`

### 6.6 POST /api/v1/capture/records/delete — 删除记录

**请求体**:`{id}`
**响应 data**:`{deleted_count}`

### 6.7 POST /api/v1/device/capture — 手动触发抓拍(MQTT 通道上传)

归 MQTT 模块实现,见 12.6 节。

---

## 7. 实时预览模块(preview,免认证)

### 7.1 GET /api/v1/preview/status — 预览状态

**响应 data**

| 字段 | 类型 | 说明 |
|---|---|---|
| preview | object | `{active, connected_clients, fps, total_frames_sent, total_bytes_sent}` |
| pipeline | object | `{initialized, running}` |
| hub | object | `{initialized, subscriber_count}` |

### 7.2 POST /api/v1/preview/start — 启动预览

**请求体**:空。启动视频流水线(自动拉起 AI 流水线)。

**响应 data**:`{success, status, pipeline_running, hub_subscribers}`

### 7.3 POST /api/v1/preview/stop — 停止预览

**请求体**:空(无订阅者时才真正停流)。

**响应 data**:`{success, status, pipeline_running}`

> 预览画面本身经 WebSocket `ws://<ip>:8081/stream` 获取,见[附录 B](#附录-b-websocket-预览流)。

---

## 8. 文件管理模块(files)

所有端点均带查询参数 **`fs`**(`flash` / `sd`,默认 `flash`)与 **`path`**(文件路径)。

### 8.1 GET /api/v1/files/list — 列目录

**查询参数**:`fs`、`path`(目录路径)

**响应 data**:`{fs, path, entries:[{name, type(file/dir), size, mtime}], count}`(出错时 `error` 字段)

### 8.2 GET /api/v1/files/download — 下载文件(信封例外)

**查询参数**:`fs`、`path`(必需)

**响应**:原始文件字节流(8 KB 分块),`Content-Disposition: attachment`。文件不存在等错误时返回信封 JSON。

### 8.3 GET /api/v1/files/preview — 预览文件

**查询参数**:`fs`、`path`

**响应**:
- 图片(≤2 MB):原始字节流,Content-Type 为图片 MIME
- 文本(≤512 KB,扩展名白名单):信封 JSON `data:{filename, mime_type, size, content(文本)}`
- 超限:`data:{too_large: true, file_size, max_preview_size}`

### 8.4 POST /api/v1/files/upload — 上传文件(流式,信封例外)

**注意:请求体为原始字节流(非 multipart)。**

| 要素 | 说明 |
|---|---|
| 查询参数 | `fs`、`path`(目标路径,含文件名,**必需**) |
| 请求体 | 原始文件字节 |
| 大小上限 | 16 MB |
| 认证 | 流式拦截器自行校验 Basic,**失败返回真实 401** |
| 其他错误 | 400(参数)/500/503,真实 HTTP 状态码 |

**响应**:信封 JSON `data:{fs, path, size}`。

### 8.5 POST /api/v1/files/create — 创建文件/目录

**请求体**:`{fs?, path, type: "file"|"dir", content?}`

**响应 data**:`{fs, path, name, type, size}`

### 8.6 DELETE /api/v1/files — 删除文件/目录

**查询参数**:`fs`、`path`

**响应 data**:`{fs, path, filename, deleted}`(目录非空返回 `DIR_NOT_EMPTY` 1408)

### 8.7 PUT /api/v1/files/rename — 重命名/移动

**请求体**:`{fs?, old_path, new_path}`
**响应 data**:`{fs, old_path, new_path}`

### 8.8 PUT /api/v1/files/edit — 编辑文本文件

**请求体**:`{fs?, path, content}`(扩展名须在文本白名单内)
**响应 data**:`{fs, path, size}`

---

## 9. AI 检测模块(ai)

### 9.1 GET /api/v1/ai/status — AI 状态

**响应 data**

| 字段 | 类型 | 说明 |
|---|---|---|
| ai_enabled | bool | AI 总开关 |
| model | object | 模型元数据:`{name, description, author, postprocess_type, input_data_type, output_data_type, color_format, version, created_at, stedgeai_version, input_width, input_height, input_channels, model_size, status(loaded/not_loaded)}` |
| pipeline | object | `{status}` |
| statistics | object | `{total_frames_processed, total_inferences, average_inference_time_ms, error_count, uptime_seconds}` |

### 9.2 POST /api/v1/ai/toggle — AI 开关

**请求体**:`{enable: bool}`
**响应 data**:`{success, message}`

### 9.3 POST /api/v1/ai/pipeline/start · /stop — 流水线启停

**请求体**:空。**响应 data**:`{success, message}`

### 9.4 GET / POST /api/v1/ai/params — AI 参数

**GET 响应 data**:`{nms_threshold, confidence_threshold, overlay_results, inference_interval_ms, descriptions:{...字段说明}}`

**POST 请求体**(均可选)

| 字段 | 类型 | 说明 |
|---|---|---|
| nms_threshold | number | NMS 阈值 |
| confidence_threshold | number | 置信度阈值 |
| overlay_results | bool | 结果叠加到抓拍图 |
| inference_interval_ms | number | 推理间隔(ms);遥测开启时 **0 会被拒绝** |

**POST 响应 data**:逐字段状态 `{<字段>: "updated"/"failed"/"invalid_range"/"rejected", current_<字段>: <当前值>, errors:[], updated:[...]}`

---

## 10. 模型管理模块(model)

### 10.1 POST /api/v1/model/validation/upload — 模型试跑验证(multipart)

**请求**:`multipart/form-data`,两部分均为 JPEG:
- `ai_image`:输入 AI 模型的图像
- `draw_image`:叠加绘制用底图

**响应 data**:`{output_image(base64 JPEG,检测结果绘制图), ai_result:{detections/poses/segments...}}`

### 10.2 POST /api/v1/model/reload — 重载 AI 模型

**请求体**:空。仅当流 hub 有订阅者时重启流水线。

**响应**:成功 `message: "OK"`;失败 `MODEL_RELOAD_FAILED`(1503)

---

## 11. 工作模式 / 电源模式(work-mode / power-mode)

### 11.1 GET /api/v1/work-mode/status — 工作模式状态

**响应 data**:`{current_mode, mode_id, configuration:{image_mode:{pir_trigger:{enable, pin_number, trigger_type}, timer_trigger:{enable, capture_mode, interval_sec, time_node_count, time_node[]}, io_trigger:{...}}, video_stream_mode:{enable, rtsp_server_url}}}`

### 11.2 POST /api/v1/work-mode/switch — 切换工作模式

**请求体**:`{mode: "image" | "video_stream"}`

**响应 data**:`{work_mode}`

### 11.3 GET /api/v1/work-mode/triggers — 触发器全量配置

**响应 data**

| 字段 | 类型 | 说明 |
|---|---|---|
| work_mode | string | 当前工作模式 |
| timer_trigger | object | `{enable, capture_mode(none/interval/once), interval_sec, time_node_count, time_node[](最多 10 个 HH:MM), weekdays[], interval_mode(scheduled…), start_time, next_capture_at}` |
| pir_trigger | object | `{enable, pin_number, trigger_type(rising_edge/falling_edge/both_edges/high_level/low_level), sensitivity_level(10–255), ignore_time_s(0–15), pulse_count(1–4), window_time_s(0–3), disable_in_preview}` |
| remote_trigger | object | `{enable}` |
| io_trigger | object | `{id, pin_number, enable, input_enable, output_enable, input_trigger_type, output_trigger_type}` |
| image_mode / video_stream_mode | object | 同 11.1 |

### 11.4 POST /api/v1/work-mode/triggers — 配置触发器

**请求体**:各触发器对象(`timer_trigger`/`pir_trigger`/`remote_trigger`/`io_trigger`)均可选,字段同 11.3。

**语义**:
- 出现的对象按携带字段部分更新;**出现但未带 `enable` 字段的对象会被禁用**
- 未出现的对象不受影响
- POST 后再 GET 一次确认实际生效值

### 11.5 POST /api/v1/work-mode/video-stream/config — 视频流模式配置

**请求体**:`{enable: bool, rtsp_server_url: string}`

### 11.6 GET /api/v1/power-mode/status — 电源模式状态

**响应 data**:`{current_mode, configuration:{current_mode, default_mode, low_power_timeout_ms, last_activity_time, mode_switch_count}}`

### 11.7 POST /api/v1/power-mode/switch — 切换电源模式

**请求体**:`{mode: "low_power" | "full_speed"}`

### 11.8 GET / POST /api/v1/power-mode/config — 电源模式配置

**GET 响应 data / POST 请求体**

| 字段 | 类型 | 说明 |
|---|---|---|
| current_mode | string | 当前模式(只读) |
| default_mode | string | 上电默认模式 |
| low_power_timeout_ms | number | 无活动进低功耗超时,**必须 > 0** |
| last_activity_time | number | 最近活动时间戳(只读) |
| mode_switch_count | number | 模式切换计数(只读) |

---

## 12. MQTT 应用模块(apps/mqtt)

### 12.1 GET /api/v1/apps/mqtt/config — MQTT 全量配置

**响应 data**(嵌套结构)

| 组 | 字段 |
|---|---|
| connection | `{hostname, port, client_id, protocol_type, protocol_version, clean_session, keepalive}` |
| authentication | `{username, password, ca_cert_path, client_cert_path, client_key_path, ca_data, client_cert_data, client_key_data, sni}` |
| last_will | `{topic, message, qos, retain}` |
| network | `{auto_reconnect, reconnect_interval_ms, connection_timeout_ms, buffer_size, outbox_limit, outbox_resend_interval_ms, outbox_expired_timeout_ms}` |
| task | `{priority, stack_size}` |
| topics | `{data_receive_topic, data_report_topic, status_topic, command_topic}` |
| qos | `{data_receive_qos, data_report_qos, status_qos, command_qos}` |
| 顶层 | `{report_content, telemetry_enabled, telemetry_topic, telemetry_qos, telemetry_format}` |
| auto_subscribe | `{auto_subscribe_receive, auto_subscribe_command}` |
| message_config | `{enable_status_report, status_report_interval_ms, enable_heartbeat, heartbeat_interval_ms}` |
| status | `{running, connected, state, version}` |

### 12.2 POST /api/v1/apps/mqtt/config — 更新 MQTT 配置

**请求体**:结构同上,顶层组对象与组内字段均可选。

**⚠ 清空语义**:请求中出现某组对象但**缺省组内字段时,该字段会被清空**(置空),并非"保持不变"。要保持某字段,必须显式回传当前值。

**响应 data**:`{message, success, connection_config_changed, action_taken, next_step}`(连接参数变化时提示需重连)

### 12.3 POST /api/v1/apps/mqtt/connect — 连接 MQTT

**请求体**:空(按当前配置)。轮询最多 5 秒。

**响应 data**:`{message, success, connected, status(connected/timeout/failed), error_code?, statistics:{total_connections, successful_connections, failed_connections, messages_published, messages_received, outbox_size}}`

未连接时发布类操作返回 `MQTT_NOT_CONNECTED`(1304)。

### 12.4 POST /api/v1/apps/mqtt/disconnect — 断开 MQTT

**请求体**:空。**响应 data**:同 12.3。

### 12.5 POST /api/v1/apps/mqtt/publish/data · /publish/status · /publish/json — 手动发布

**请求体**

| 端点 | 字段 |
|---|---|
| /publish/data | `{data: string}` → 发到 data_report_topic |
| /publish/status | `{status: string}` → 发到 status_topic |
| /publish/json | `{json_data: string}` → 原样发布 |

**响应 data**:`{message, message_id, success}`

### 12.6 POST /api/v1/device/capture — 手动抓拍并经 MQTT 上传

**请求体**(均可选)

| 字段 | 类型 | 说明 |
|---|---|---|
| enable_ai | bool | 抓拍同时跑 AI |
| store_to_sd | bool | 存 SD |
| chunk_size | number | 分片大小 |

**响应 data**:`{success, code, result, message, enable_ai, store_to_sd, chunk_size, duration_ms}`

**错误**:设备忙 `OPERATION_IN_PROGRESS`(1705);MQTT 未连 `MQTT_NOT_CONNECTED`(1304);超时 `OPERATION_TIMEOUT`(1701)

---

## 13. RTMP 推流模块(apps/rtmp)

### 13.1 GET /api/v1/apps/rtmp/config — RTMP 配置

**响应 data**:`{enable, url, stream_key, ...}`

### 13.2 POST /api/v1/apps/rtmp/config — 更新配置

**请求体**:`{enable?, url?, stream_key?}`(均可选)

### 13.3 POST /api/v1/apps/rtmp/start — 启动推流

**请求体**:空。软失败:推流失败时信封仍 `success:true` 但 `data.success=false`。
**响应 data**:`{success, message, state}`

### 13.4 POST /api/v1/apps/rtmp/stop — 停止推流

**响应 data**:同 13.3。

### 13.5 GET /api/v1/apps/rtmp/status — 推流状态

**响应 data**:`{enabled, port, auth_mode, message, state(idle/connecting/streaming/reconnecting/stopping/error)}`

---

## 14. RTSP 服务模块(apps/rtsp)

### 14.1 GET /api/v1/apps/rtsp/config — RTSP 配置

**响应 data**:`{enabled, port, auth_mode, username, password}`
> 密码**明文**返回(无脱敏)。

### 14.2 PUT /api/v1/apps/rtsp/config — 更新配置

**请求体**:`{enabled?, port?, auth_mode?, username?, password?}`

- `password` 传 `"******"` 表示**保持不变**(掩码哨兵)
- `enabled` 与 RTMP 互斥:开 RTSP 会关 RTMP;反向不对称
- **响应 data**:`{enabled, port, auth_mode, message}`

### 14.3 GET /api/v1/apps/rtsp/status — 服务状态

**响应 data**:`{running, stream_url}`

### 14.4 GET /api/v1/apps/rtsp/clients — 客户端列表

**响应 data**:`[{id, ip, port, connected_duration}]`(id=会话标识)

### 14.5 DELETE /api/v1/apps/rtsp/clients/* — 踢指定客户端

**路径**:`/api/v1/apps/rtsp/clients/<会话id>`(通配路由)
**响应 data**:`{success, message}`(无论会话是否存在均成功)

---

## 15. Webhook 模块(apps/webhook)

### 15.1 GET /api/v1/apps/webhook/config — 配置

**响应 data**:`{enable, url, auth_type, secret, has_custom_ca}`

### 15.2 POST /api/v1/apps/webhook/config — 更新配置

**请求体**:`{enable?, url?, auth_type?, secret?}`
**响应 data**:`{success, message}`

### 15.3 POST /api/v1/apps/webhook/test — 测试推送

**请求体**:空(按当前配置发一条测试消息)。
**响应 data**:`{success, message}`(软失败)

### 15.4 GET / POST / DELETE /api/v1/apps/webhook/ca-cert — 自定义 CA 证书

- **GET** `data`:`{ca_cert_data(PEM 文本), ca_cert_length}`
- **POST**:`{ca_cert_data}`,必须以 `-----BEGIN` 开头,**≤ 8 KB**
- **DELETE**:清除自定义 CA,恢复默认

**响应 data**:`{success, message}`

---

## 16. 固件升级模块(system/ota)

**共享约定**

- `firmwareType` 取值:`fsbl` / `app` / `web` / `ai_default`(AI 槽 1)/ `ai`(实际活跃 AI 槽)/ `wifi` / `reserved2`,默认 `app`
- 包头 1 KB(`ota_header_t`);AI 类型另带 1 KB 模型包头(共 2048 B)
- 型号戳:包/Bundle 标注 `device_model ≠ 0 且 ≠ 0x3010` 一律拒绝(0=旧包放行)
- 超时:单包上传空闲 5 分钟、Bundle 会话空闲 10 分钟自动终止并恢复服务
- 上传期间自动停止 MQTT、AI 流水线、相机(等待在途抓拍 ≤30 秒),结束后恢复;**任何路径均不自动重启**

### 16.1 POST /api/v1/system/ota/precheck — 固件包头预检

**请求**:原始二进制体 = 包前 1024 B(AI 类型 2048 B),`Content-Type` 不强制。
**查询参数**:`firmwareType`(默认 app)、`contentLength`(整包大小,给出则须与头一致)

**响应 data**:`{valid: true, message, firmware_size, firmware_version, firmware_crc32, part_table_changed, device_table_crc, package_table_crc}`

**失败**:`OTA_HEADER_VALIDATION_FAILED`(1806),且 `data:{valid:false, reason}`

### 16.2 POST /api/v1/system/ota/upload — 流式固件上传

**⚠ 信封例外**:该端点在路由分发前被流式拦截器接管(自行校验认证,返回**真实 HTTP 状态码**)。

| 要素 | 说明 |
|---|---|
| 请求体 | **原始固件字节流**(非 multipart/JSON),1 KB 头 + 载荷 |
| Content-Length | **必需**,1024 B – 100 MB |
| 查询参数 | `firmwareType`(默认 app) |
| 直接烧写模式 | `direct=1&addr=0x<地址>`,仅在 Bundle 会话(`bundle/begin`)有效,须与设备解析的计划地址一致 |
| 互斥 | 普通上传与 Bundle 会话互斥(进行中一方拒绝另一方) |

**处理**:头校验(magic/CRC/型号/类型/大小)→ 逐 1 KB 写 flash 并滚动 CRC32 → 末尾校验整包 CRC。FSBL 载荷先暂存 PSRAM,CRC 通过后才擦写。WEB 类型上传后立即重载 Web 资源;AI 类型写 NVS 活跃槽标志;WiFi 类型暂存 `.rps` 于 `WIFI_FW_BASE`,重启时推送。

**响应**:信封 JSON;成功 `message: "Upgrade successful"`。失败真实状态码 400(头校验失败/OTA 进行中/参数)/500(内存/flash/CRC 错误)。

### 16.3 POST /api/v1/system/ota/upgrade-local — 升级确认(WiFi 推送待命)

**请求体**:`{firmware_type: "wifi"}`(其他值/空体为 no-op)
**行为**:`wifi` 时置 NVS 更新标志,下次重启把暂存的 `.rps` 推给 917 主机。
**响应 data**:`null`,`message: "OK"`

### 16.4 POST /api/v1/system/ota/export — 导出固件(信封例外)

**请求体**(JSON,Content-Type 必须为 application/json)

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| firmware_type | string | 是 | 见共享约定;`ai` 解析为实际活跃槽 |
| filename | string | 是 | 导出文件名(用于 Content-Disposition) |

**响应**:原始固件字节流(`application/octet-stream` + `Content-Disposition`),1 KB 分块。

### 16.5 POST /api/v1/system/ota/bundle/precheck — 整机 Bundle 预检

一键重刷包(4 KB 头 + 各子包)。**请求体 = Bundle 文件前 4096 字节**(原始二进制)。

**校验**:头 magic/大小/CRC、型号戳、条目表、分区表物理合法(无重叠、不越界、FSBL 不可移动)、类型合法且无重复、必含分区、条目不超分区、改布局包必须含 FSBL。

**响应 data**

```jsonc
{
  "layout_changed": false,
  "layout_diff": [ { "part": "...", "device_base": 0, "device_size": 0,
                     "bundle_base": 0, "bundle_size": 0 } ],   // 仅 layout_changed 时
  "total_bytes": 0,
  "entries": [ {
      "type": "app",            // fsbl|app|web|ai|wifi
      "fw_type": 2,             // 原始码: 0x01 fsbl / 0x02 app / 0x03 web / 0x04,0x05 AI / 0x08 wifi
      "skippable": true,        // 版本相同且分区未动(FSBL 另需布局未变)
      "cur_version": "…", "new_version": "…",
      "size": 0,                // 子包字节数
      "offset": 4096,           // 在 Bundle 文件内的字节偏移
      "addr": 0                 // 设备侧解析出的绝对烧写地址
  } ]
}
```

**副作用**:预检结果驻留为全局 Bundle 会话(`prechecked=true`),10 分钟空闲过期。

### 16.6 POST /api/v1/system/ota/bundle/begin — 启动 Bundle 强刷会话

**请求体**:`{skip?: ["wifi", "fsbl", ...]}`——跳过清单,名字必须在计划内且 `skippable=true`;**全部跳过会被拒绝**。

**响应 data**:`{burn_count}`,`message: "Bundle session started"`

**⚠ 副作用**:每次 begin 都会**擦除 OTA info 分区**,下次开机按新布局重建 SystemState(槽 A/APP1/AI_1 活跃)。跳过 `wifi` 会清除 WiFi 待推送标志。

### 16.7 POST /api/v1/system/ota/bundle/finish — 结束/中止 Bundle 会话

**请求体**:`{result: "ok" | "abort"}`(非 "ok" 一律按 abort)
**行为**:`ok` 且计划含 WiFi 更新时置 `wifi_mark_update_pending()`;会话清空,**不自动重启**(由前端随后调 `/system/restart`)。已烧写的固件不回滚,abort 幂等。
**响应 data**:`{wifi_pending}`,`message: "Bundle finished"/"Bundle aborted"`

---

## 17. ISP 图像质量模块(isp)

**共享错误码**(业务码 2001–2008,见 1.5.2):2001 未初始化 / 2002 参数非法(JSON)/ 2003 超范围 / 2004 依赖错误 / 2005 HAL / 2006 传感器 / 2007 算法 / 2008 忙。

**通用语义**:GET 返回当前值;PUT **立即生效于 ISP 硬件**并更新 IQ 参数缓存,但**不直接写 NVS**——需 `POST /isp/config/save` 持久化。GET 额外携带 `_meta`(字段元数据:类型/单位/范围/默认值)。

### 17.1 GET /api/v1/isp/params — 全量参数

**响应 data**:`{sensor_info:{name,bayer_pattern,color_depth,width,height}, aec, awb, demosaicing, stat_removal, black_level, bad_pixel, isp_gain, color_conv, contrast, gamma, stat_area, sensor_delay}`(各组字段同下述对应端点)

### 17.2 GET /api/v1/isp/sensor — 传感器信息

**响应 data**:`{name, bayer_pattern(RGGB/GRBG/GBRG/BGGR/MONO), color_depth, width, height, gain_range:{min,max}, exposure_range:{min,max}}`(增益单位 mB,曝光单位 µs)

### 17.3 GET /api/v1/isp/statistics — ISP 统计

**响应 data**:`{down:{average_r,average_g,average_b,average_l,histogram[12]}, up:{average_r,average_g,average_b,average_l}, estimated_lux, frame_id}`(down=ISP 处理后,up=处理前)

### 17.4 GET / PUT /api/v1/isp/aec — 自动曝光

| 字段 | 类型 | 范围 | PUT |
|---|---|---|---|
| enable | bool | — | ✅ |
| exposure_compensation | number | −4…+4(0.5EV 步进,即 ±2.0EV) | ✅ |
| anti_flicker_freq | number | 0 / 50 / 60 | ❌(只读,仅 import 可改) |

GET 另含 `current_state:{sensor_gain, sensor_exposure, estimated_lux}`。

### 17.5 GET / PUT /api/v1/isp/aec/manual — 手动曝光

| 字段 | 类型 | 范围 | 说明 |
|---|---|---|---|
| gain | number | sensor.gain_min…max | 单位 mB |
| exposure | number | sensor.exposure_min…max | 单位 µs |

> 手动曝光仅在 AEC **禁用**时生效(依赖说明见 GET `_meta.dependencies`)。

### 17.6 GET / PUT /api/v1/isp/awb — 自动白平衡

| 字段 | 类型 | 说明 |
|---|---|---|
| enable | bool | AWB 开关 |
| mode | string | `auto`(按场景选 profile)/ `manual`(按 color_temp) |
| color_temp | number | manual 模式色温(PUT 随 mode 一起消费) |
| profiles | array | 恰好 5 个:`{index(0–4), label(≤31 字符), reference_color_temp, isp_gain:{r,g,b}, ccm[3][3](±4.0×), reference_rgb:{r,g,b}}` |

GET 另含 `current_color_temp, current_gains:{r,g,b}, current_ccm:{enable, matrix}`。profiles 数值无范围校验,直写 IQ 缓存。

### 17.7 GET / PUT /api/v1/isp/demosaicing — 去马赛克

`{enable, peak, line_v, line_h, edge}`(强度均 0–7,默认 4);`bayer_type` 只读(传感器决定)。

### 17.8 GET / PUT /api/v1/isp/stat_removal — 统计行剔除

`{enable, head_lines(0–7), valid_lines(0–4094)}`

### 17.9 GET / PUT /api/v1/isp/black_level — 黑电平

`{enable, r, g, b}`(各 0–255,默认 12)

### 17.10 GET / PUT /api/v1/isp/bad_pixel — 坏点校正

`{enable, strength(0–7), detected_count(只读,运行时检出数), algo_enable, algo_threshold(≥0)}`

### 17.11 GET / PUT /api/v1/isp/gain — ISP 数字增益

`{enable, r, g, b}`,单位 **100000000 = 1.0×**,最大 16.0×(1,600,000,000)。
> AWB 使能时 ISP 增益由自动控制接管。

### 17.12 GET / PUT /api/v1/isp/color_conv — 色彩转换矩阵

`{enable, matrix[3][3]}`,系数 ±399,000,000(1e8=1.0×);行/列必须恰好 3×3。

### 17.13 GET / PUT /api/v1/isp/contrast — 对比度 LUT

`{enable, lut[9]}`,每项 0–394(对应亮度锚点 [0,32,…,256],100=1.0×);必须恰好 9 项。

### 17.14 GET / PUT /api/v1/isp/gamma — Gamma 校正

`{enable}`(唯一字段)

### 17.15 GET / PUT /api/v1/isp/stat_area — 统计窗口

`{x, y, width, height}`,width/height ≥ 4 且 ≤ 4094;须满足 `x+width ≤ sensor_width`、`y+height ≤ sensor_height`。PUT 与当前值合并。

### 17.16 GET / PUT /api/v1/isp/lux_ref — 照度参考(AEC 估算输入)

`{high_lux:{lux_ref,expo1,lum1,expo2,lum2}, low_lux:{…同…}, calib_factor(float)}`;lum1/lum2 为 0–255。仅写 IQ 缓存。

### 17.17 GET / PUT /api/v1/isp/sensor_delay — 传感器延迟

`{delay}`(int ≥ 0,存 uint8)。仅写 IQ 缓存。

### 17.18 POST /api/v1/isp/config/save — 持久化 IQ 参数到 NVS

**请求体**:空。保存当前全部实时 IQ 参数。**响应**:`message: "ISP configuration saved successfully"`。

### 17.19 POST /api/v1/isp/config/load — 从 NVS 加载

**请求体**:空。无保存配置时报 2001(`No saved ISP configuration found`)。
> ⚠ 当前代码中实际应用调用被注释,仅校验配置存在即返回成功文案,以固件行为为准。

### 17.20 GET /api/v1/isp/config/export — 导出 IQ 配置

**响应 data**:`{export_version:"1.0", export_timestamp, config:{stat_removal, demosaicing{+type}, contrast, stat_area, bad_pixel{algo_* ,static_*}, black_level, aec, awb{profiles[{label,color_temp,gain_r,gain_g,gain_b,ccm[9]平铺}]}, isp_gain, color_conv{matrix[9]平铺}, gamma, sensor{gain,exposure,delay}, lux_ref{hl_*,ll_*,calib_factor}}}`

> 导出字段名与单参数 API 略有差异(`color_temp`/`gain_r|g|b`/平铺 9 元素数组)。

### 17.21 POST /api/v1/isp/config/import — 导入 IQ 配置

**请求体**:`{config: {…同 export 结构…}}`(`config` 必需,内部各节可选;`ccm`/`matrix` 接受 ≥9 元素平铺数组)

**行为**:实时应用 + 持久化 NVS + 刷新设备缓存。**不做范围校验**(与单参数 PUT 不同)。
**响应**:`message: "ISP configuration imported and applied successfully"`

---

## 附录 A. curl 快速上手

```bash
# 所有请求都带 Basic 认证(用户名 admin)
curl -u admin:<密码> http://<设备IP>/api/v1/device/info

# 判错看 success 字段,不是 HTTP 状态码
curl -s -u admin:12345678 http://<设备IP>/api/v1/ai/status | jq .success

# 上传文件(原始字节流,非 multipart)
curl -u admin:<密码> -X POST --data-binary @frame.jpg \
  "http://<设备IP>/api/v1/files/upload?fs=sd&path=/photos/frame.jpg"

# OTA 上传(流式,真实状态码)
curl -u admin:<密码> -X POST --data-binary @app_v4.3.1.61.bin \
  "http://<设备IP>/api/v1/system/ota/upload?firmwareType=app"
```

## 附录 B. WebSocket 预览流

| 项 | 值 |
|---|---|
| 地址 | `ws://<设备IP>:8081/stream`(独立监听,默认端口 8081) |
| 数据 | H.264 视频帧;GOP ≈ 1 秒 |
| 关键帧同步 | 服务端为每个新订阅者做关键帧 join-sync:关键帧到达时拼接 `[SPS][PPS][IDR]` 单帧发送,晚连客户端不会黑屏 |
| 生命周期 | `POST /api/v1/preview/start` 启动流水线;最后一个客户端断开后 `POST /api/v1/preview/stop` 真正停流 |

> 播放端建议使用支持 Annex-B 起始码的解码器(如 jmuxer);首帧即含参数集,无需额外请求 SPS/PPS。

## 附录 C. 版本与文档维护

- 本文档由 `e42ea18` 工作区代码生成(2026-09-03),固件版本 **4.3.1.61**。
- 版本宏位于 `Custom/Common/Inc/version.h`(构建时自动生成);期望配套版本 `EXPECTED_FSBL_VERSION_STRING`、`EXPECTED_WIFI_VERSION_STRING`。
- 各模块路由注册于 `Custom/Services/Web/web_service.c`,路由表与错误码见 `web_server.h`、`api_business_error.h`、`api_isp_module.h`。
- 固件升级后请对照 `git diff` 复核接口变化并同步更新本文档。



