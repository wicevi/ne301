# NE301 HTTP API Reference

| Item | Value |
|---|---|
| **Firmware version (APP)** | **v4.3.1.61** |
| Firmware build time | 2026-09-03 10:13:26 |
| Git commit | `e42ea18-dirty` (branch `main`) |
| Document date | 2026-09-03 |
| Source | Generated from `Custom/Services/Web/` in the workspace above |

> ⚠️ This document matches workspace `e42ea18` (including uncommitted changes). If the firmware differs from this description, the firmware implementation prevails.

## 0. Software Version Information

| Component | Version | Notes |
|---|---|---|
| APP (main firmware) | **4.3.1.61** | auto-generated in `Custom/Common/Inc/version.h` |
| FSBL (first-stage bootloader) | 1.0.3.1 (expected) | `EXPECTED_FSBL_VERSION_STRING`; actual compared at runtime |
| WiFi / Si91x firmware | 2.16.5.108 (expected) | `EXPECTED_WIFI_VERSION_STRING`; actual compared at runtime |
| Web assets (frontend) | 1.5.0.6 | shipped with the release bundle; query at runtime via `/device/firmware-versions` |
| AI model package | 4.0.0.0 | same |
| WakeCore | 0.2.7.5 | same |

At runtime, `GET /api/v1/device/firmware-versions` returns the **actual running versions** of every component, and `GET /api/v1/device/version-check` reports whether FSBL/WiFi firmware match what the APP expects.

---

## 1. General Conventions

### 1.1 Service & Base URL

| Item | Value |
|---|---|
| HTTP service | Mongoose, listening on **TCP port 80** |
| HTTPS | Disabled (`IS_HTTPS=0`) |
| API base path | **`/api/v1`** |
| Max request body | 5 MB (normal requests through the router) |
| Connection | `Connection: close`, one request per connection |
| Static assets | Web UI served from `/` (ETag/304/gzip supported) |

Device address:
- STA / Ethernet mode: the IP obtained by the device (check the router/AP)
- AP mode: the device hotspot gateway address (default `10.10.10.1` subnet, subject to actual device configuration)

### 1.2 Authentication

- **HTTP Basic auth** per request; no sessions/cookies.
- Username is fixed to **`admin`** (`auth_mgr.h: AUTH_ADMIN_USERNAME`); the password is the device access password.
- The password can be verified via `POST /api/v1/login` and changed via `POST /api/v1/change-password`.

**Unauthenticated endpoints** (route table `require_auth=false`):

| Endpoint | Purpose |
|---|---|
| `POST /api/v1/login` | password verification |
| `GET /api/v1/preview/status` | preview status |
| `POST /api/v1/preview/start` | start preview |
| `POST /api/v1/preview/stop` | stop preview |

> Note: `POST /api/v1/system/ota/upload` is intercepted by the stream processor before route dispatch and handles auth itself — see §16.2.

**Auth failure response** (normal endpoints): HTTP status is still **200** with envelope `error_code: "UNAUTHORIZED"`. The stream upload endpoints (`files/upload`, `ota/upload`) are exceptions and return a real `401`.

### 1.3 Unified Response Envelope

All endpoints routed through the router return **HTTP 200 regardless of success or failure**, with a JSON envelope:

```jsonc
// success
{
  "success": true,
  "message": "Device information retrieved successfully",   // optional
  "data": { ... }                                            // optional
}

// failure
{
  "success": false,
  "error_code": "INVALID_PASSWORD",   // present only on failure
  "message": "Invalid password"
}
```

| Field | Type | Notes |
|---|---|---|
| success | bool | business outcome |
| error_code | string | present only on failure; upper-snake-case name (see 1.5) |
| message | string | human-readable description; may appear on success too |
| data | object/array/null | business payload; may be absent |

> **Clients must branch on the `success` field — never on the HTTP status code.**

### 1.4 Envelope Exceptions (real HTTP statuses / non-JSON)

| Endpoint | Response form |
|---|---|
| `GET /api/v1/files/download` | raw file bytes + `Content-Disposition` (404/400 still envelope JSON) |
| `GET /api/v1/files/preview` | raw bytes for images; JSON envelope for text files |
| `POST /api/v1/files/upload` | streamed; auth failure **401**, bad request 400, internal 500/503 (real statuses) |
| `POST /api/v1/system/ota/upload` | streamed; real statuses 400/401/500 |
| `POST /api/v1/system/ota/export` | raw firmware bytes + `Content-Disposition` |
| `/` static assets | 200/304/404 |
| `OPTIONS` preflight | 200 + CORS headers |

### 1.5 Error Codes

#### 1.5.1 Framework codes (web_server.h `api_error_code_t`)

| HTTP semantic | error_code string | Trigger |
|---|---|---|
| 400 | `INVALID_REQUEST` | missing/invalid parameters, JSON parse failure, wrong Content-Type |
| 401 | `UNAUTHORIZED` | Basic auth failure |
| 404 | `NOT_FOUND` | path not registered |
| 405 | `METHOD_NOT_ALLOWED` | method mismatch (method match is case-insensitive) |
| 500 | `INTERNAL_ERROR` | internal error |
| 503 | `SERVICE_UNAVAILABLE` | required service not running |

#### 1.5.2 Business codes (api_business_error.h, from 1001)

| Code | error_code string | Meaning |
|---|---|---|
| 1001 | `INVALID_PASSWORD` | invalid password |
| 1002 | `INVALID_CREDENTIALS` | invalid username or password |
| 1003 | `UNAUTHORIZED` | unauthorized |
| 1004 | `SESSION_EXPIRED` | session expired |
| 1005 | `TOKEN_INVALID` | invalid token |
| 1007 | `PERMISSION_DENIED` | permission denied |
| 1101 | `INVALID_PARAM` | invalid parameter |
| 1102 | `MISSING_PARAM` | missing required parameter |
| 1103 | `PARAM_OUT_OF_RANGE` | parameter out of range |
| 1104 | `INVALID_FORMAT` | invalid data format |
| 1201 | `DEVICE_OFFLINE` | device offline |
| 1202 | `DEVICE_BUSY` | device busy |
| 1204 | `CAMERA_ERROR` | camera error |
| 1206 | `HARDWARE_ERROR` | hardware error |
| 1301 | `NETWORK_ERROR` | network error |
| 1302 | `NETWORK_TIMEOUT` | network timeout |
| 1304 | `MQTT_NOT_CONNECTED` | MQTT not connected |
| 1305 | `WIFI_NOT_CONNECTED` | WiFi not connected |
| 1401 | `STORAGE_FULL` | storage full |
| 1403 | `FILE_NOT_FOUND` | file not found |
| 1407 | `INSUFFICIENT_SPACE` | insufficient storage space |
| 1408 | `DIR_NOT_EMPTY` | directory not empty (cannot delete) |
| 1501 | `MODEL_NOT_LOADED` | AI model not loaded |
| 1502 | `MODEL_INVALID` | invalid AI model |
| 1503 | `MODEL_RELOAD_FAILED` | model reload failed |
| 1505 | `INFERENCE_TIMEOUT` | inference timeout |
| 1601 | `CONFIG_INVALID` | invalid configuration |
| 1602 | `CONFIG_NOT_FOUND` | configuration not found |
| 1604 | `CONFIG_UPDATE_FAILED` | configuration update failed |
| 1701 | `OPERATION_TIMEOUT` | operation timeout |
| 1702 | `OPERATION_FAILED` | operation failed |
| 1705 | `OPERATION_IN_PROGRESS` | operation already in progress (busy) |
| 1706 | `AT_COMMAND_FAILED` | AT command failed (cellular modem) |
| 1801 | `FIRMWARE_INVALID` | invalid firmware |
| 1804 | `OTA_IN_PROGRESS` | OTA upgrade in progress |
| 1805 | `OTA_FAILED` | OTA upgrade failed |
| 1806 | `OTA_HEADER_VALIDATION_FAILED` | OTA/bundle header validation failed |
| 1901 | `RESOURCE_NOT_FOUND` | resource not found |
| 1902 | `RESOURCE_BUSY` | resource busy |
| 2001–2008 | `ISP_*` (see §17) | ISP business errors (2001 not initialized / 2002 invalid param / 2003 out of range / 2004 dependency / 2005 HAL / 2006 sensor / 2007 algorithm / 2008 busy) |
| 9999 | `UNKNOWN` | unknown error |

### 1.6 CORS & Preflight

All responses carry CORS headers (`Access-Control-Allow-Origin: *`, etc.); `OPTIONS` preflight is answered directly with 200 + CORS headers without entering business routing.

### 1.7 Live Preview Stream (WebSocket, not part of the HTTP API)

The preview stream runs on a separate WebSocket service: **`ws://<device-ip>:8081/stream`** (default port 8081, path `/stream`). See [Appendix B](#appendix-b-websocket-preview-stream).

---

## 2. Endpoint Overview

15 modules, ~150 endpoints, all under `/api/v1`. All require Basic auth unless marked "no auth".

| # | Module | Prefix | Endpoints |
|---|---|---|---|
| 3 | Authentication | `/login`, `/change-password` | 2 |
| 4 | Device & system | `/device/*`, `/system/*` | 24 |
| 5 | Network | `/system/network/*`, `/wifi/*`, `/halow/*`, `/comm/*`, `/cellular/*`, `/poe/*` | 47 |
| 6 | Capture & upload | `/capture/*` | 7 |
| 7 | Live preview | `/preview/*` (no auth) | 3 |
| 8 | File management | `/files/*` | 8 |
| 9 | AI detection | `/ai/*` | 6 |
| 10 | Model management | `/model/*` | 2 |
| 11 | Work mode / power mode | `/work-mode/*`, `/power-mode/*` | 9 |
| 12 | MQTT | `/apps/mqtt/*`, `/device/capture` | 8 |
| 13 | RTMP streaming | `/apps/rtmp/*` | 5 |
| 14 | RTSP service | `/apps/rtsp/*` | 5 |
| 15 | Webhook | `/apps/webhook/*` | 6 |
| 16 | Firmware upgrade (OTA) | `/system/ota/*` | 7 |
| 17 | ISP image quality | `/isp/*` | 27 |
| — | Static assets / Web UI | `/` | — |

> The 10 HaLow endpoints are registered only when the firmware is built with HaLow enabled (`NETIF_WIFI_HALOW_IS_ENABLE`); this build has it enabled.

---

## 3. Authentication Module (auth)

### 3.1 POST /api/v1/login — verify device password

**No auth.** Used by the login page to verify the password (username is fixed `admin` and not sent).

**Request body**

| Field | Type | Required | Description |
|---|---|---|---|
| password | string | yes | device access password |

**Response data**: `null` (success carries only `success:true` + message)

**Errors**: `INVALID_REQUEST` (missing param); `INVALID_PASSWORD` (1001, wrong password)

### 3.2 POST /api/v1/change-password — change device password

**Request body**

| Field | Type | Required | Description |
|---|---|---|---|
| password | string | yes | new password, **8–32 characters** |

**Response data**: `null`

**Notes**: takes effect immediately; subsequent requests must use the new password for Basic auth.

**Errors**: `INVALID_REQUEST` (missing/invalid length); `INVALID_PASSWORD` (change failed)

---

## 4. Device & System Module (device / system)

### 4.1 GET /api/v1/device/info — device information

**Response data**

| Field | Type | Description |
|---|---|---|
| device_name | string | device name |
| mac_address | string | MAC address |
| serial_number | string | serial number |
| hardware_version | string | hardware version |
| software_version | string | software version (the APP firmware version) |
| camera_module | string | camera module |
| extension_modules | string | extension modules |
| power_supply_type | string | power supply type |
| battery_percent | number | battery percentage |
| communication_type | string | current communication type |
| storage_card_info | string | storage card info |
| storage_usage_percent | number | storage usage (%) |

### 4.2 GET /api/v1/device/firmware-versions — component firmware versions

**Response data**

| Field | Type | Description |
|---|---|---|
| fsbl | string | actual running FSBL version |
| app | string | actual running APP version |
| web | string | web asset version |
| model | string | AI model package version |
| wakecore | string | WakeCore version |
| wifi | string | Si91x WiFi/HaLow firmware version |
| wifi_running | string | WiFi firmware running-state marker |
| expected_fsbl | string | FSBL version expected by the APP |
| expected_wifi | string | WiFi firmware version expected by the APP |

### 4.3 GET /api/v1/device/version-check — FSBL/WiFi version consistency

**Response data**

| Field | Type | Description |
|---|---|---|
| fsbl_mismatch | bool | FSBL actual ≠ expected |
| wifi_mismatch | bool | WiFi firmware actual ≠ expected |
| expected_fsbl / current_fsbl | string | expected / actual FSBL version |
| expected_wifi / current_wifi | string | expected / actual WiFi firmware version |

### 4.4 GET /api/v1/device/storage — storage information

**Response data** (SD card + flash partitions)

| Field | Type | Description |
|---|---|---|
| sd_card_connected | bool | SD card present |
| total_capacity_mb / available_capacity_mb / used_capacity_mb | number | SD capacity (MB) |
| usage_percent | number | SD usage (%) |
| total_capacity_gb / available_capacity_gb / used_capacity_gb | number | SD capacity (GB) |
| cyclic_overwrite_enabled | bool | cyclic overwrite switch |
| overwrite_threshold_percent | number | overwrite threshold (%) |
| flash_fs_mounted | bool | flash filesystem mounted |
| flash_fs_error | bool | flash filesystem error |
| flash_error | string | flash error description |
| flash_total_capacity_mb / flash_available_capacity_mb / flash_used_capacity_mb | number | flash capacity (MB) |
| flash_usage_percent | number | flash usage (%) |
| flash_total_capacity_gb / flash_available_capacity_gb / flash_used_capacity_gb | number | flash capacity (GB) |
| flash_fs_type | string | flash filesystem type (littlefs) |
| status | string | storage status summary |
| primary_storage | string | primary storage (sd / flash) |

### 4.5 POST /api/v1/device/storage/config — cyclic overwrite configuration

**Request body**

| Field | Type | Required | Description |
|---|---|---|---|
| cyclic_overwrite_enabled | bool | no | cyclic overwrite switch |
| overwrite_threshold_percent | number | no | overwrite threshold |

**Response data**: echoes `{cyclic_overwrite_enabled, overwrite_threshold_percent}`

### 4.6 POST /api/v1/device/storage/format — format storage

**Request body**

| Field | Type | Required | Description |
|---|---|---|---|
| target | string | no | format target (`sd` / `flash`, default `sd`) |

**Response**: executed asynchronously; poll progress via `/device/storage`.

### 4.7 POST /api/v1/device/name — rename device

**Request body**: `{device_name: string}`
**Response data**: echoes `{device_name}`

### 4.8 GET / POST /api/v1/device/image/config — image configuration

GET returns the current config; POST is a partial update (send only changed fields).

**Fields** (GET returns / POST optional)

| Field | Type | Range/values | Description |
|---|---|---|---|
| brightness | number | 0–100 | brightness |
| contrast | number | 0–100 | contrast |
| horizontal_flip | bool | — | horizontal flip |
| vertical_flip | bool | — | vertical flip |
| aec | number | 0–100 | auto-exposure target |
| isp_mode | number | 0 (outdoor) / 1 (indoor) / 255 (custom) | ISP scene mode |
| grayscale | bool | — | grayscale output |
| fast_capture_skip_frames | number | 0–300 | fast-capture skip frames |
| fast_capture_resolution | number | 0 (1280×720) / 1 (1920×1080) / 2 (2688×1520) | fast-capture resolution |
| fast_capture_jpeg_quality | number | 1–100 | fast-capture JPEG quality |
| capture_disable_comm | bool | — | pause communication during capture |
| capture_storage_ai | bool | — | store AI-overlay capture images |

**Notes**: POST of brightness/contrast/flip/aec automatically restarts the AI pipeline (stop then start).

### 4.9 GET / POST /api/v1/device/sys-clk/config — CPU clock profile

Persisted by FSBL; **takes effect after reboot**.

**GET response data**

| Field | Type | Description |
|---|---|---|
| valid | bool | whether the config read out validly |
| sys_clk_profile | number | current clock profile |

**POST request body**

| Field | Type | Required | Description |
|---|---|---|---|
| sys_clk_profile | number | yes | 1=HSE 200 MHz, 2=HSE 400 MHz, 3=HSI 800 MHz, 4=HSE 800 MHz |

### 4.10 GET / POST /api/v1/device/light/config — fill light configuration

**GET response data**

| Field | Type | Description |
|---|---|---|
| connected | bool | fill light connected |
| mode | string | `off` / `on` / `auto` / `custom` |
| brightness_level | number | brightness 0–100 |
| fill_light_while_streaming | bool | work-time fill light mode (light on unconditionally per config while the device runs) |
| custom_schedule | object | timed-on schedule `{start_hour, start_minute, end_hour, end_minute}` |

**POST request body** (all optional)

| Field | Type | Description |
|---|---|---|
| mode | string | `off`/`on`/`auto`/`custom` |
| brightness_level | number | 0–100 |
| fill_light_while_streaming | bool | as above |
| custom_schedule | object | same as GET |

### 4.11 GET / POST /api/v1/device/camera/config — camera configuration

**Fields**

| Field | Type | Description |
|---|---|---|
| enabled | bool | camera enable |
| width / height | number | resolution |
| fps | number | frame rate |
| image_config | object | full image configuration, fields as in 4.8 |

### 4.12 POST /api/v1/system/time — set system time

**Request body**

| Field | Type | Required | Description |
|---|---|---|---|
| utc_timestamp | number | one of the two | UTC epoch seconds |
| local_timestamp | number | one of the two | local epoch seconds |
| timezone_offset_hours | number | no | timezone offset (hours) |

**Response data**: `{message, utc_timestamp, local_timestamp, timezone_offset_hours, utc_time, local_time}`

**Notes**: the device also syncs time via SNTP/U0; the web path goes through the RTC scheduler (with step guards).

### 4.13 GET /api/v1/system/logs — read logs

**Query params**: `file` (log file name, optional, default main log)

**Response data**: `{content (log text), size}`; for the file-level list see 4.14.

### 4.14 GET /api/v1/system/logs/export — export logs

**Response data**: `{log_files: [{filename, size, content}], total_files, total_size}`

### 4.15 POST /api/v1/system/restart — restart device

**Request body**: `{delay_seconds?: number}` (default 3)

**Response data**: `{delay_seconds}` (executed asynchronously by a background task)

### 4.16 POST /api/v1/system/factory-reset — factory reset

**Request body**: `{delay_seconds?: number}`

**Response data**: `{restart_delay_seconds}` (async: wipes config, then auto-restarts)

### 4.17 GET /api/v1/device/config/export — export device configuration

**Response data**

| Field | Type | Description |
|---|---|---|
| export_version | string | export format version |
| export_timestamp | number | export timestamp |
| config | object | structured config (per module) |
| config_raw | string | raw config text |

### 4.18 POST /api/v1/device/config/import — import device configuration

**Request body**: the `data` exported by 4.17 as-is (or just the `config` object).

**Response data**: `{message, config_version, timestamp, checksum, saved_to_file}`

### 4.19 GET / POST /api/v1/device/preference/stream_tab — Web UI preference

**GET response data**: `{stream_tab: string}` (the stream tab remembered by the frontend)
**POST request body**: `{stream_tab: string}`

---

## 5. Network Module (network)

### 5.1 GET /api/v1/system/network/status — communication overview

**Response data**

| Field | Type | Description |
|---|---|---|
| service_state | string | communication service state |
| service_version | string | service version |
| active_type | string | currently active comm type (wifi/cellular/poe/halow…) |
| selected_type | string | selected type |
| current_comm_type | string | current comm type |
| has_connection | bool | any usable connection |
| active_display_name / selected_display_name / current_comm_display_name | string | display names |
| available_comm_types | array | `[{type, display_name, is_selected, is_active, is_connected, is_current}]` |
| available_comm_count | number | number of available types |
| preferred_type | string | preferred type |
| auto_priority | bool | automatic priority selection |
| is_auto_selection | bool | automatic selection mode |
| selection_mode | string | selection mode |
| current_comm_info | object | `{status, ip_address, signal_strength, ssid?, isp?, message}` |

### 5.2 GET /api/v1/system/network/wifi/sta — WiFi STA status

**Response data**: `{connected, ssid, bssid, rssi, channel, ip_address, mac_address, state}`

### 5.3 GET /api/v1/system/network/wifi/ap — WiFi AP status

**Response data**: `{enabled, state, ip_address, mac_address, channel, ssid, password, ap_sleep_time, security}`

> The AP password is returned in clear text (no masking on the device side).

### 5.4 POST /api/v1/wifi/config — configure and connect WiFi (alias: POST /system/network/wifi)

**Request body**

| Field | Type | Required | Description |
|---|---|---|---|
| interface | string | no | `ap` (configure the hotspot) / `wl` (connect as STA), default STA |
| ssid | string | yes | SSID, 1–31 chars |
| password | string | yes | password, 8–63 chars |
| bssid | string | no | pin a specific BSSID |
| ap_sleep_time | number | no | AP idle sleep time 0–3600 s (when interface=ap) |
| use_saved_password | bool | no | connect with the saved password |

**Response data**: `{message, interface, ssid, ap_sleep_time}` (connection is async; poll 5.1/5.2)

### 5.5 POST /api/v1/wifi/scan — scan WiFi (alias: POST /system/network/scan)

**Request body**: empty JSON or `{}` (may include `force`)

**Response data** (async; the first call returns scanning-in-progress, poll again for results)

| Field | Type | Description |
|---|---|---|
| scan_results | object | `{known_networks[], unknown_networks[], known_count, unknown_count}` |
| scan_in_progress | bool | scan running |

Scan entry fields:

| Field | Description |
|---|---|
| ssid / bssid | SSID / BSSID |
| rssi | signal strength |
| channel | channel |
| security | security type (open/wpa2…) |
| connected | currently connected |
| is_known / quick_connect | saved / quick-connect capable |
| last_connected_time | last connected timestamp |

### 5.6 POST /api/v1/wifi/disconnect — disconnect WiFi (alias: POST /system/network/disconnect)

**Request body**: `{interface: "wl"}`. Response `data:{message, interface, if_name, status}`

### 5.7 POST /api/v1/wifi/delete — delete a saved network (alias: POST /system/network/delete)

**Request body**: `{ssid}` (interface optional). Response `data:{status, message}`

### 5.8 GET /api/v1/wifi/region — WiFi region code

**Response data**: `{region, active_region, supported_regions[]}` (only us/eu/jp/kr/cn are valid)

### 5.9 PUT /api/v1/wifi/region — set WiFi region code

**Request body**: `{region}`. **Response data**: `{region, restart_required, message}` (some region codes require a WiFi restart to take effect)

### 5.10 HaLow group (`/halow/*`, 10 endpoints)

| Endpoint | Method | Description |
|---|---|---|
| /api/v1/halow/sta | GET | HaLow STA state: `{connected, ssid, bssid, rssi, channel, ip_address, netmask, gateway, ip_mode, mac_address, state, region, supported_regions[]}` |
| /api/v1/halow/region | GET | `{region, supported_regions[]}` |
| /api/v1/halow/region | PUT | `{region}` → `{region, message, scan_required}` |
| /api/v1/halow/scan | POST | trigger a scan; response entry structure as in 5.5 |
| /api/v1/halow | POST | connect: `{ssid, password?, bssid?, security?, region?, use_saved_password?}` → `{message, interface, ssid, bssid}` |
| /api/v1/halow/disconnect | POST | `{message, interface, status}` |
| /api/v1/halow/delete | POST | `{ssid}` delete a saved network |
| /api/v1/halow/ip | GET/POST | IP configuration: `{ip_mode(dhcp/static), ip_address, netmask, gateway}` |
| /api/v1/halow/radio | GET | radio parameters (below) |
| /api/v1/halow/radio | PUT | radio parameters (below) |

**radio fields**: `{tx_power_dbm, scan_dwell_ms, rate_mcs(-1..9), rate_bw_mhz(-1/1/2/4/8), rate_gi(-1..1), ps_mode(0/1), limits:{tx_power_dbm:{min,max}, scan_dwell_ms:{min,max,default}}}`

> `ps_mode` is the power-save switch. HaLow and PoE are hardware-mutually-exclusive (EXTI15); switching is coordinated automatically by the communication service — no need to down the other side manually.

### 5.11 Communication type group (`/comm/*`, 4 endpoints)

| Endpoint | Method | Description |
|---|---|---|
| /api/v1/comm/types | GET | full type list: `{current_type, preferred_type, auto_priority, types:[{type, status, available, connected, can_switch, is_current, ip_address, signal_strength, priority, display_name, detail:{imei?, apn?, isp?, ssid?, rssi?, channel?, interface?}}], available_types[], type_count, current_type_info:{type, status, ip_address, signal_strength, ssid?, isp?, network_type?, csq?, signal_level?}}` |
| /api/v1/comm/switch | POST | `{type}` (wifi/cellular/poe/halow) switch, **blocking up to 30 s** → `{success, from_type, to_type, switch_time_ms, error?}` |
| /api/v1/comm/prefer | GET/POST | preferred type: `{preferred_type, auto_priority}` |
| /api/v1/comm/priority | POST | priority adjustment → `{current_type, success}` |

### 5.12 Cellular group (`/cellular/*`, 9 endpoints)

| Endpoint | Method | Description |
|---|---|---|
| /api/v1/cellular/status | GET | `{available, status, message, imei, settings:{apn, username, password, pin_code, authentication, enable_roaming, operator, plmn}}` |
| /api/v1/cellular/info | GET | modem details: `{network_status, sim_status, model, version, imei, imsi, iccid, isp, network_type, register_status, plmn_id, lac, cell_id, signal_level, csq, csq_level, rssi, ipv4_address, ipv4_gateway, ipv4_dns, ipv6_address, ipv6_gateway, ipv6_dns, connection_duration_sec, error?}` |
| /api/v1/cellular/settings | GET/POST | fields as in status.settings; POST fields all optional |
| /api/v1/cellular/save | POST | persist settings to NVS → `{message, apn, enable_roaming}` |
| /api/v1/cellular/connect | POST | dial up → `{success, status}` |
| /api/v1/cellular/disconnect | POST | disconnect → `{success, status}` |
| /api/v1/cellular/refresh | POST | refresh modem state → `{success, status}` |
| /api/v1/cellular/at | POST | send a raw AT command: `{command}` → `{success, command, response}`; failure returns `AT_COMMAND_FAILED` (1706) |

### 5.13 PoE group (`/poe/*`, 8 endpoints)

| Endpoint | Method | Description |
|---|---|---|
| /api/v1/poe/status | GET | `{available, status, message, ip_address, connected}` |
| /api/v1/poe/info | GET | link details: `{available, network_status, status_code, status_message, ip_mode, ip_address, netmask, gateway, dns_primary, dns_secondary, hostname, mac_address, interface_name, link_up, poe_powered, connection_duration_sec, connection_start_time, dhcp_lease_time, dhcp_lease_remaining, connect_count, disconnect_count, dhcp_fail_count, last_error_code, error?}` |
| /api/v1/poe/config | GET | `{ip_mode, ip_address, netmask, gateway, dns_primary, dns_secondary, hostname, dhcp_timeout_ms, dhcp_retry_count, dhcp_retry_interval_ms, power_recovery_delay_ms, auto_reconnect, persist_last_ip, validate_gateway, detect_ip_conflict}` |
| /api/v1/poe/config | POST | same fields, all optional → `{message, ip_mode}` |
| /api/v1/poe/connect | POST | `{success, status}` |
| /api/v1/poe/disconnect | POST | `{success, status}` |
| /api/v1/poe/validate | POST | validate the current network config: `{ip_conflict, gateway_reachable, valid, errors[], warnings[]}` |
| /api/v1/poe/apply / /api/v1/poe/save | POST | apply/save config → `{success, status, ip_mode}` |

---

## 6. Capture & Upload Module (capture)

### 6.1 GET / POST /api/v1/capture/upload-config — capture/upload configuration

**GET response data / POST request body** (POST fields all optional, partial update)

| Field | Type | Values | Description |
|---|---|---|---|
| version | number | — | config version (read-only) |
| mode | string | `instant` / `batch` / `scheduled` / `local_only` | upload mode |
| storage | string | `auto` / `flash` / `sd` / `none` | capture storage location |
| policy | string | `wrap` (cyclic overwrite) / `stop` (stop writing) | full-disk policy |
| upload_protocol | string | `mqtt` / `webhook` | upload channel |
| retry_max_attempts | number | ≥0 | max upload retries |
| batch_count | number | — | batch size |
| schedule_minutes | number[] | 0–59 | schedule minute table (scheduled mode) |
| keep_sent_hours | number | — | retention of sent records (hours) |
| max_pending_records | number | — | pending record cap (SD) |
| flash_max_records | number | — | flash record cap |
| upload_network | string | — | restrict upload network |

**Constraint**: `storage=none` requires `mode=instant` (otherwise 400).

**POST response data**: `{success}` or per-field echo.

### 6.2 GET /api/v1/capture/queue — upload queue status

**Response data**

| Field | Type | Description |
|---|---|---|
| mode / storage | string | current mode/storage |
| actual_fs | string | actually active filesystem |
| pending_count | number | pending count |
| sent_count | number | sent count |
| failed_count | number | failed count |
| local_count | number | local-only count |
| bytes_used_kb / bytes_available_kb | number | capacity |
| next_scheduled_at | number | next scheduled upload timestamp |

### 6.3 GET /api/v1/capture/records — query capture records

**Query params**

| Param | Description |
|---|---|
| state | filter by state (pending/sent/failed/local) |
| offset / limit | pagination, limit ≤ 200 |
| from / to | timestamp range |
| sort | sort order |

**Response data**: `{state, offset, limit, total, records:[{id, state, timestamp, size, retry_count, trigger, last_error}]}`

### 6.4 DELETE /api/v1/capture/records — clear records

**Response data**: `{reset_count}`

### 6.5 POST /api/v1/capture/records/retry — retry failed records

**Request body**: `{id}` (single) or `{all_failed: true}`
**Response data**: `{success, id}`

### 6.6 POST /api/v1/capture/records/delete — delete a record

**Request body**: `{id}`
**Response data**: `{deleted_count}`

### 6.7 POST /api/v1/device/capture — manual capture (MQTT upload)

Implemented by the MQTT module — see §12.6.

---

## 7. Live Preview Module (preview, no auth)

### 7.1 GET /api/v1/preview/status — preview status

**Response data**

| Field | Type | Description |
|---|---|---|
| preview | object | `{active, connected_clients, fps, total_frames_sent, total_bytes_sent}` |
| pipeline | object | `{initialized, running}` |
| hub | object | `{initialized, subscriber_count}` |

### 7.2 POST /api/v1/preview/start — start preview

**Request body**: empty. Starts the video pipeline (the AI pipeline is pulled up automatically).

**Response data**: `{success, status, pipeline_running, hub_subscribers}`

### 7.3 POST /api/v1/preview/stop — stop preview

**Request body**: empty (the stream actually stops only when there are no subscribers).

**Response data**: `{success, status, pipeline_running}`

> The preview frames themselves are consumed via WebSocket `ws://<ip>:8081/stream` — see [Appendix B](#appendix-b-websocket-preview-stream).

---

## 8. File Management Module (files)

Every endpoint takes the query parameters **`fs`** (`flash` / `sd`, default `flash`) and **`path`**.

### 8.1 GET /api/v1/files/list — list a directory

**Query params**: `fs`, `path` (directory path)

**Response data**: `{fs, path, entries:[{name, type(file/dir), size, mtime}], count}` (`error` field on failure)

### 8.2 GET /api/v1/files/download — download a file (envelope exception)

**Query params**: `fs`, `path` (required)

**Response**: raw file bytes (streamed in 8 KB chunks) with `Content-Disposition: attachment`. Missing-file and similar errors return envelope JSON.

### 8.3 GET /api/v1/files/preview — preview a file

**Query params**: `fs`, `path`

**Response**:
- Image (≤2 MB): raw bytes with an image Content-Type
- Text (≤512 KB, extension whitelist): envelope JSON `data:{filename, mime_type, size, content}`
- Too large: `data:{too_large: true, file_size, max_preview_size}`

### 8.4 POST /api/v1/files/upload — upload a file (streamed, envelope exception)

**Note: the request body is raw bytes (not multipart).**

| Item | Description |
|---|---|
| Query params | `fs`, `path` (target path incl. file name, **required**) |
| Request body | raw file bytes |
| Size cap | 16 MB |
| Auth | verified by the stream interceptor itself; **failure returns a real 401** |
| Other errors | 400 (params) / 500 / 503, real HTTP statuses |

**Response**: envelope JSON `data:{fs, path, size}`.

### 8.5 POST /api/v1/files/create — create a file/directory

**Request body**: `{fs?, path, type: "file"|"dir", content?}`

**Response data**: `{fs, path, name, type, size}`

### 8.6 DELETE /api/v1/files — delete a file/directory

**Query params**: `fs`, `path`

**Response data**: `{fs, path, filename, deleted}` (non-empty directory returns `DIR_NOT_EMPTY` 1408)

### 8.7 PUT /api/v1/files/rename — rename/move

**Request body**: `{fs?, old_path, new_path}`
**Response data**: `{fs, old_path, new_path}`

### 8.8 PUT /api/v1/files/edit — edit a text file

**Request body**: `{fs?, path, content}` (extension must be in the text whitelist)
**Response data**: `{fs, path, size}`

---

## 9. AI Detection Module (ai)

### 9.1 GET /api/v1/ai/status — AI status

**Response data**

| Field | Type | Description |
|---|---|---|
| ai_enabled | bool | AI master switch |
| model | object | model metadata: `{name, description, author, postprocess_type, input_data_type, output_data_type, color_format, version, created_at, stedgeai_version, input_width, input_height, input_channels, model_size, status(loaded/not_loaded)}` |
| pipeline | object | `{status}` |
| statistics | object | `{total_frames_processed, total_inferences, average_inference_time_ms, error_count, uptime_seconds}` |

### 9.2 POST /api/v1/ai/toggle — AI on/off

**Request body**: `{enable: bool}`
**Response data**: `{success, message}`

### 9.3 POST /api/v1/ai/pipeline/start · /stop — pipeline start/stop

**Request body**: empty. **Response data**: `{success, message}`

### 9.4 GET / POST /api/v1/ai/params — AI parameters

**GET response data**: `{nms_threshold, confidence_threshold, overlay_results, inference_interval_ms, descriptions:{...per-field descriptions}}`

**POST request body** (all optional)

| Field | Type | Description |
|---|---|---|
| nms_threshold | number | NMS threshold |
| confidence_threshold | number | confidence threshold |
| overlay_results | bool | overlay results onto captured images |
| inference_interval_ms | number | inference interval (ms); **0 is rejected while telemetry is on** |

**POST response data**: per-field status `{<field>: "updated"/"failed"/"invalid_range"/"rejected", current_<field>: <value>, errors:[], updated:[...]}`

---

## 10. Model Management Module (model)

### 10.1 POST /api/v1/model/validation/upload — model dry-run validation (multipart)

**Request**: `multipart/form-data` with two JPEG parts:
- `ai_image`: the image fed to the AI model
- `draw_image`: the base image for overlay drawing

**Response data**: `{output_image (base64 JPEG with detections drawn), ai_result:{detections/poses/segments...}}`

### 10.2 POST /api/v1/model/reload — reload the AI model

**Request body**: empty. Restarts the pipeline only if the stream hub has subscribers.

**Response**: success `message: "OK"`; failure `MODEL_RELOAD_FAILED` (1503)

---

## 11. Work Mode / Power Mode (work-mode / power-mode)

### 11.1 GET /api/v1/work-mode/status — work-mode status

**Response data**: `{current_mode, mode_id, configuration:{image_mode:{pir_trigger:{enable, pin_number, trigger_type}, timer_trigger:{enable, capture_mode, interval_sec, time_node_count, time_node[]}, io_trigger:{...}}, video_stream_mode:{enable, rtsp_server_url}}}`

### 11.2 POST /api/v1/work-mode/switch — switch work mode

**Request body**: `{mode: "image" | "video_stream"}`

**Response data**: `{work_mode}`

### 11.3 GET /api/v1/work-mode/triggers — full trigger configuration

**Response data**

| Field | Type | Description |
|---|---|---|
| work_mode | string | current work mode |
| timer_trigger | object | `{enable, capture_mode(none/interval/once), interval_sec, time_node_count, time_node[] (up to 10 × HH:MM), weekdays[], interval_mode(scheduled…), start_time, next_capture_at}` |
| pir_trigger | object | `{enable, pin_number, trigger_type(rising_edge/falling_edge/both_edges/high_level/low_level), sensitivity_level(10–255), ignore_time_s(0–15), pulse_count(1–4), window_time_s(0–3), disable_in_preview}` |
| remote_trigger | object | `{enable}` |
| io_trigger | object | `{id, pin_number, enable, input_enable, output_enable, input_trigger_type, output_trigger_type}` |
| image_mode / video_stream_mode | object | as in 11.1 |

### 11.4 POST /api/v1/work-mode/triggers — configure triggers

**Request body**: trigger objects (`timer_trigger`/`pir_trigger`/`remote_trigger`/`io_trigger`) all optional, fields as in 11.3.

**Semantics**:
- a present object is partially updated by the fields it carries; **a present object without an `enable` field is DISABLED**
- absent objects are untouched
- POST then GET again to confirm the actually applied values

### 11.5 POST /api/v1/work-mode/video-stream/config — video-stream mode config

**Request body**: `{enable: bool, rtsp_server_url: string}`

### 11.6 GET /api/v1/power-mode/status — power-mode status

**Response data**: `{current_mode, configuration:{current_mode, default_mode, low_power_timeout_ms, last_activity_time, mode_switch_count}}`

### 11.7 POST /api/v1/power-mode/switch — switch power mode

**Request body**: `{mode: "low_power" | "full_speed"}`

### 11.8 GET / POST /api/v1/power-mode/config — power-mode configuration

**GET response data / POST request body**

| Field | Type | Description |
|---|---|---|
| current_mode | string | current mode (read-only) |
| default_mode | string | power-on default mode |
| low_power_timeout_ms | number | inactivity timeout into low power, **must be > 0** |
| last_activity_time | number | last activity timestamp (read-only) |
| mode_switch_count | number | mode switch counter (read-only) |

---

## 12. MQTT Application Module (apps/mqtt)

### 12.1 GET /api/v1/apps/mqtt/config — full MQTT configuration

**Response data** (nested structure)

| Group | Fields |
|---|---|
| connection | `{hostname, port, client_id, protocol_type, protocol_version, clean_session, keepalive}` |
| authentication | `{username, password, ca_cert_path, client_cert_path, client_key_path, ca_data, client_cert_data, client_key_data, sni}` |
| last_will | `{topic, message, qos, retain}` |
| network | `{auto_reconnect, reconnect_interval_ms, connection_timeout_ms, buffer_size, outbox_limit, outbox_resend_interval_ms, outbox_expired_timeout_ms}` |
| task | `{priority, stack_size}` |
| topics | `{data_receive_topic, data_report_topic, status_topic, command_topic}` |
| qos | `{data_receive_qos, data_report_qos, status_qos, command_qos}` |
| top level | `{report_content, telemetry_enabled, telemetry_topic, telemetry_qos, telemetry_format}` |
| auto_subscribe | `{auto_subscribe_receive, auto_subscribe_command}` |
| message_config | `{enable_status_report, status_report_interval_ms, enable_heartbeat, heartbeat_interval_ms}` |
| status | `{running, connected, state, version}` |

### 12.2 POST /api/v1/apps/mqtt/config — update MQTT configuration

**Request body**: same structure; top-level group objects and inner fields all optional.

**⚠ Clearing semantics**: if a group object is present but an inner field is **absent, that field is CLEARED** (emptied) — it does not keep its previous value. To keep a field, send its current value explicitly.

**Response data**: `{message, success, connection_config_changed, action_taken, next_step}` (prompts a reconnect when connection parameters changed)

### 12.3 POST /api/v1/apps/mqtt/connect — connect MQTT

**Request body**: empty (uses the current config). Polls for up to 5 s.

**Response data**: `{message, success, connected, status(connected/timeout/failed), error_code?, statistics:{total_connections, successful_connections, failed_connections, messages_published, messages_received, outbox_size}}`

Publishing operations while disconnected return `MQTT_NOT_CONNECTED` (1304).

### 12.4 POST /api/v1/apps/mqtt/disconnect — disconnect MQTT

**Request body**: empty. **Response data**: as in 12.3.

### 12.5 POST /api/v1/apps/mqtt/publish/data · /publish/status · /publish/json — manual publish

**Request body**

| Endpoint | Field |
|---|---|
| /publish/data | `{data: string}` → published to data_report_topic |
| /publish/status | `{status: string}` → published to status_topic |
| /publish/json | `{json_data: string}` → published verbatim |

**Response data**: `{message, message_id, success}`

### 12.6 POST /api/v1/device/capture — manual capture uploaded via MQTT

**Request body** (all optional)

| Field | Type | Description |
|---|---|---|
| enable_ai | bool | run AI on the captured frame |
| store_to_sd | bool | store to SD |
| chunk_size | number | chunk size |

**Response data**: `{success, code, result, message, enable_ai, store_to_sd, chunk_size, duration_ms}`

**Errors**: busy `OPERATION_IN_PROGRESS` (1705); MQTT down `MQTT_NOT_CONNECTED` (1304); timeout `OPERATION_TIMEOUT` (1701)

---

## 13. RTMP Streaming Module (apps/rtmp)

### 13.1 GET /api/v1/apps/rtmp/config — RTMP configuration

**Response data**: `{enable, url, stream_key, ...}`

### 13.2 POST /api/v1/apps/rtmp/config — update configuration

**Request body**: `{enable?, url?, stream_key?}` (all optional)

### 13.3 POST /api/v1/apps/rtmp/start — start streaming

**Request body**: empty. Soft failure: on stream failure the envelope is still `success:true` with `data.success=false`.
**Response data**: `{success, message, state}`

### 13.4 POST /api/v1/apps/rtmp/stop — stop streaming

**Response data**: as in 13.3.

### 13.5 GET /api/v1/apps/rtmp/status — streaming status

**Response data**: `{enabled, port, auth_mode, message, state(idle/connecting/streaming/reconnecting/stopping/error)}`

---

## 14. RTSP Service Module (apps/rtsp)

### 14.1 GET /api/v1/apps/rtsp/config — RTSP configuration

**Response data**: `{enabled, port, auth_mode, username, password}`
> The password is returned **in clear text** (no masking).

### 14.2 PUT /api/v1/apps/rtsp/config — update configuration

**Request body**: `{enabled?, port?, auth_mode?, username?, password?}`

- `password` = `"******"` means **keep unchanged** (mask sentinel)
- `enabled` is mutually exclusive with RTMP: enabling RTSP disables RTMP; the reverse is asymmetric
- **Response data**: `{enabled, port, auth_mode, message}`

### 14.3 GET /api/v1/apps/rtsp/status — service status

**Response data**: `{running, stream_url}`

### 14.4 GET /api/v1/apps/rtsp/clients — client list

**Response data**: `[{id, ip, port, connected_duration}]` (id = session identifier)

### 14.5 DELETE /api/v1/apps/rtsp/clients/* — kick a client

**Path**: `/api/v1/apps/rtsp/clients/<session-id>` (wildcard route)
**Response data**: `{success, message}` (succeeds whether or not the session exists)

---

## 15. Webhook Module (apps/webhook)

### 15.1 GET /api/v1/apps/webhook/config — configuration

**Response data**: `{enable, url, auth_type, secret, has_custom_ca}`

### 15.2 POST /api/v1/apps/webhook/config — update configuration

**Request body**: `{enable?, url?, auth_type?, secret?}`
**Response data**: `{success, message}`

### 15.3 POST /api/v1/apps/webhook/test — test push

**Request body**: empty (sends a test message per the current config).
**Response data**: `{success, message}` (soft failure)

### 15.4 GET / POST / DELETE /api/v1/apps/webhook/ca-cert — custom CA certificate

- **GET** `data`: `{ca_cert_data (PEM text), ca_cert_length}`
- **POST**: `{ca_cert_data}`; must start with `-----BEGIN`, **≤ 8 KB**
- **DELETE**: clears the custom CA, restoring the default

**Response data**: `{success, message}`

---

## 16. Firmware Upgrade Module (system/ota)

**Shared conventions**

- `firmwareType` values: `fsbl` / `app` / `web` / `ai_default` (AI slot 1) / `ai` (actually active AI slot) / `wifi` / `reserved2`; default `app`
- Package header is 1 KB (`ota_header_t`); AI types carry an extra 1 KB model header (2048 B total)
- Device-model stamp: a package/bundle stamped `device_model ≠ 0 and ≠ 0x3010` is rejected (0 = unstamped legacy, allowed)
- Timeouts: single-upload idle 5 min, bundle-session idle 10 min — auto-terminated, services restarted
- During upload the service automatically stops MQTT, the AI pipeline and the camera (waits ≤30 s for an in-flight capture) and restores them afterwards; **no path auto-reboots**

### 16.1 POST /api/v1/system/ota/precheck — firmware header precheck

**Request**: raw binary body = the first 1024 B of the package (2048 B for AI types); `Content-Type` not enforced.
**Query params**: `firmwareType` (default app), `contentLength` (full package size; if given it must equal the header's `total_package_size`)

**Response data**: `{valid: true, message, firmware_size, firmware_version, firmware_crc32, part_table_changed, device_table_crc, package_table_crc}`

**Failure**: `OTA_HEADER_VALIDATION_FAILED` (1806) with `data:{valid:false, reason}`

### 16.2 POST /api/v1/system/ota/upload — streamed firmware upload

**⚠ Envelope exception**: this endpoint is intercepted by the stream processor before route dispatch (auth handled there; **real HTTP statuses** returned).

| Item | Description |
|---|---|
| Request body | **raw firmware bytes** (not multipart/JSON), 1 KB header + payload |
| Content-Length | **required**, 1024 B – 100 MB |
| Query params | `firmwareType` (default app) |
| Direct-burn mode | `direct=1&addr=0x<addr>`; valid only during a bundle session (`bundle/begin`); `addr` must exactly equal the device-resolved plan address |
| Mutual exclusion | normal upload and bundle sessions are mutually exclusive (an active one rejects the other) |

**Processing**: header verification (magic/CRC/model/type/size) → flash writes in 1 KB chunks with a running CRC32 → final whole-package CRC check. FSBL payloads are staged in PSRAM and flashed only after the CRC passes. WEB uploads reload the web assets immediately; AI uploads set the NVS active-slot flag; WiFi uploads stage the `.rps` at `WIFI_FW_BASE`, pushed at the next reboot.

**Response**: envelope JSON; success `message: "Upgrade successful"`. Failures use real statuses 400 (header verification failed / OTA in progress / params) / 500 (memory/flash/CRC errors).

### 16.3 POST /api/v1/system/ota/upgrade-local — upgrade confirmation (arms WiFi push)

**Request body**: `{firmware_type: "wifi"}` (any other value or an empty body is a no-op)
**Behavior**: for `wifi`, sets the NVS update flag so the next reboot pushes the staged `.rps` to the 917 host.
**Response data**: `null`, `message: "OK"`

### 16.4 POST /api/v1/system/ota/export — export firmware (envelope exception)

**Request body** (JSON; Content-Type must be application/json)

| Field | Type | Required | Description |
|---|---|---|---|
| firmware_type | string | yes | see shared conventions; `ai` resolves to the actually active slot |
| filename | string | yes | export file name (used in Content-Disposition) |

**Response**: raw firmware bytes (`application/octet-stream` + `Content-Disposition`), streamed in 1 KB chunks.

### 16.5 POST /api/v1/system/ota/bundle/precheck — full-reflash bundle precheck

One-click full-reflash bundle (4 KB header + sub-packages). **Request body = the first 4096 bytes of the bundle file** (raw binary).

**Validation**: header magic/size/CRC; device-model stamp; entry table; partition layout physically valid (no overlap, within flash, FSBL must not move); legal types without duplicates; required partitions present; entries fit their partitions; a layout-changing bundle must contain FSBL.

**Response data**

```jsonc
{
  "layout_changed": false,
  "layout_diff": [ { "part": "...", "device_base": 0, "device_size": 0,
                     "bundle_base": 0, "bundle_size": 0 } ],   // only when layout_changed
  "total_bytes": 0,
  "entries": [ {
      "type": "app",            // fsbl|app|web|ai|wifi
      "fw_type": 2,             // raw code: 0x01 fsbl / 0x02 app / 0x03 web / 0x04,0x05 AI / 0x08 wifi
      "skippable": true,        // same version AND unmoved partition (FSBL additionally needs unchanged layout)
      "cur_version": "…", "new_version": "…",
      "size": 0,                // sub-package bytes
      "offset": 4096,           // byte offset within the bundle file
      "addr": 0                 // device-resolved absolute flash burn address
  } ]
}
```

**Side effects**: the validated header + plan is stored as the global bundle session (`prechecked=true`); expires after 10 idle minutes.

### 16.6 POST /api/v1/system/ota/bundle/begin — start the bundle forced-reflash session

**Request body**: `{skip?: ["wifi", "fsbl", ...]}` — skip list; names must be in the plan and `skippable=true`; **skipping everything is rejected**.

**Response data**: `{burn_count}`, `message: "Bundle session started"`

**⚠ Side effect**: every begin **erases the OTA info partition**; the next boot rebuilds SystemState from the new layout (slot A / APP1 / AI_1 active). Skipping `wifi` clears the pending-WiFi-push flag.

### 16.7 POST /api/v1/system/ota/bundle/finish — finish/abort the bundle session

**Request body**: `{result: "ok" | "abort"}` (anything other than "ok" is treated as abort)
**Behavior**: on `"ok"` with a WiFi update still in the plan, calls `wifi_mark_update_pending()`; the session is cleared either way, **no auto-reboot** (the frontend then calls `/system/restart`). Already-burned firmware is not rolled back; abort is idempotent.
**Response data**: `{wifi_pending}`, `message: "Bundle finished"/"Bundle aborted"`

---

## 17. ISP Image Quality Module (isp)

**Shared error codes** (business codes 2001–2008, see 1.5.2): 2001 not initialized / 2002 invalid param (JSON) / 2003 out of range / 2004 dependency / 2005 HAL / 2006 sensor / 2007 algorithm / 2008 busy.

**General semantics**: GET returns current values; PUT **applies live to the ISP hardware** and updates the IQ-parameter cache but **does not write NVS by itself** — persist via `POST /isp/config/save`. GETs additionally carry `_meta` (field metadata: type/unit/range/default).

### 17.1 GET /api/v1/isp/params — all parameters

**Response data**: `{sensor_info:{name,bayer_pattern,color_depth,width,height}, aec, awb, demosaicing, stat_removal, black_level, bad_pixel, isp_gain, color_conv, contrast, gamma, stat_area, sensor_delay}` (group fields as in the corresponding endpoints below)

### 17.2 GET /api/v1/isp/sensor — sensor information

**Response data**: `{name, bayer_pattern(RGGB/GRBG/GBRG/BGGR/MONO), color_depth, width, height, gain_range:{min,max}, exposure_range:{min,max}}` (gain in mB, exposure in µs)

### 17.3 GET /api/v1/isp/statistics — ISP statistics

**Response data**: `{down:{average_r,average_g,average_b,average_l,histogram[12]}, up:{average_r,average_g,average_b,average_l}, estimated_lux, frame_id}` (down = after ISP processing, up = before)

### 17.4 GET / PUT /api/v1/isp/aec — auto exposure

| Field | Type | Range | PUT |
|---|---|---|---|
| enable | bool | — | ✅ |
| exposure_compensation | number | −4…+4 (0.5 EV steps, i.e. ±2.0 EV) | ✅ |
| anti_flicker_freq | number | 0 / 50 / 60 | ❌ (read-only here; settable only via import) |

GET also carries `current_state:{sensor_gain, sensor_exposure, estimated_lux}`.

### 17.5 GET / PUT /api/v1/isp/aec/manual — manual exposure

| Field | Type | Range | Description |
|---|---|---|---|
| gain | number | sensor.gain_min…max | unit mB |
| exposure | number | sensor.exposure_min…max | unit µs |

> Manual exposure takes effect only while AEC is **disabled** (see GET `_meta.dependencies`).

### 17.6 GET / PUT /api/v1/isp/awb — auto white balance

| Field | Type | Description |
|---|---|---|
| enable | bool | AWB switch |
| mode | string | `auto` (profile by scene) / `manual` (by color_temp) |
| color_temp | number | manual-mode color temp (PUT consumes it together with `mode`) |
| profiles | array | exactly 5: `{index(0–4), label(≤31 chars), reference_color_temp, isp_gain:{r,g,b}, ccm[3][3](±4.0×), reference_rgb:{r,g,b}}` |

GET also carries `current_color_temp, current_gains:{r,g,b}, current_ccm:{enable, matrix}`. Profile numbers have no range validation and are written straight into the IQ cache.

### 17.7 GET / PUT /api/v1/isp/demosaicing — demosaicing

`{enable, peak, line_v, line_h, edge}` (strengths all 0–7, default 4); `bayer_type` read-only (sensor-determined).

### 17.8 GET / PUT /api/v1/isp/stat_removal — statistics line removal

`{enable, head_lines(0–7), valid_lines(0–4094)}`

### 17.9 GET / PUT /api/v1/isp/black_level — black level

`{enable, r, g, b}` (each 0–255, default 12)

### 17.10 GET / PUT /api/v1/isp/bad_pixel — bad-pixel correction

`{enable, strength(0–7), detected_count(read-only, runtime-detected), algo_enable, algo_threshold(≥0)}`

### 17.11 GET / PUT /api/v1/isp/gain — ISP digital gain

`{enable, r, g, b}`, unit **100000000 = 1.0×**, max 16.0× (1,600,000,000).
> ISP gain is auto-controlled while AWB is enabled.

### 17.12 GET / PUT /api/v1/isp/color_conv — color conversion matrix

`{enable, matrix[3][3]}`, coefficients ±399,000,000 (1e8 = 1.0×); must be exactly 3×3.

### 17.13 GET / PUT /api/v1/isp/contrast — contrast LUT

`{enable, lut[9]}`, each 0–394 (luma anchors [0,32,…,256], 100 = 1.0×); must be exactly 9 items.

### 17.14 GET / PUT /api/v1/isp/gamma — gamma correction

`{enable}` (the only field)

### 17.15 GET / PUT /api/v1/isp/stat_area — statistics window

`{x, y, width, height}`, width/height ≥ 4 and ≤ 4094; must satisfy `x+width ≤ sensor_width`, `y+height ≤ sensor_height`. PUT merges with current values.

### 17.16 GET / PUT /api/v1/isp/lux_ref — lux reference (AEC estimation input)

`{high_lux:{lux_ref,expo1,lum1,expo2,lum2}, low_lux:{…same…}, calib_factor(float)}`; lum1/lum2 are 0–255. IQ cache only.

### 17.17 GET / PUT /api/v1/isp/sensor_delay — sensor delay

`{delay}` (int ≥ 0, stored as uint8). IQ cache only.

### 17.18 POST /api/v1/isp/config/save — persist IQ parameters to NVS

**Request body**: empty. Saves all current live IQ parameters. **Response**: `message: "ISP configuration saved successfully"`.

### 17.19 POST /api/v1/isp/config/load — load from NVS

**Request body**: empty. Without a saved config returns 2001 (`No saved ISP configuration found`).
> ⚠ In the current code the actual apply call is commented out; the handler only verifies a config exists and then reports success — firmware behavior prevails.

### 17.20 GET /api/v1/isp/config/export — export IQ configuration

**Response data**: `{export_version:"1.0", export_timestamp, config:{stat_removal, demosaicing{+type}, contrast, stat_area, bad_pixel{algo_*, static_*}, black_level, aec, awb{profiles[{label,color_temp,gain_r,gain_g,gain_b,ccm[9 flat]}]}, isp_gain, color_conv{matrix[9 flat]}, gamma, sensor{gain,exposure,delay}, lux_ref{hl_*,ll_*,calib_factor}}}`

> Export field names differ slightly from the per-parameter API (`color_temp`/`gain_r|g|b`/flat 9-element arrays).

### 17.21 POST /api/v1/isp/config/import — import IQ configuration

**Request body**: `{config: {…same structure as export…}}` (`config` required; inner sections optional; `ccm`/`matrix` accept flat arrays of ≥9 items)

**Behavior**: applies live + persists to NVS + refreshes the device cache. **No range validation** (unlike the individual PUTs).
**Response**: `message: "ISP configuration imported and applied successfully"`

---

## Appendix A. curl Quick Start

```bash
# Every request carries Basic auth (username admin)
curl -u admin:<password> http://<device-ip>/api/v1/device/info

# Branch on the success field, not the HTTP status
curl -s -u admin:12345678 http://<device-ip>/api/v1/ai/status | jq .success

# File upload (raw byte stream, not multipart)
curl -u admin:<password> -X POST --data-binary @frame.jpg \
  "http://<device-ip>/api/v1/files/upload?fs=sd&path=/photos/frame.jpg"

# OTA upload (streamed, real status codes)
curl -u admin:<password> -X POST --data-binary @app_v4.3.1.61.bin \
  "http://<device-ip>/api/v1/system/ota/upload?firmwareType=app"
```

## Appendix B. WebSocket Preview Stream

| Item | Value |
|---|---|
| URL | `ws://<device-ip>:8081/stream` (separate listener, default port 8081) |
| Payload | H.264 video frames; GOP ≈ 1 s |
| Keyframe sync | the server performs per-subscriber keyframe join-sync: on each keyframe it sends a single `[SPS][PPS][IDR]` composite frame, so late-joining clients never see a black screen |
| Lifecycle | `POST /api/v1/preview/start` starts the pipeline; after the last client disconnects, `POST /api/v1/preview/stop` actually stops the stream |

> Players should use a decoder that accepts Annex-B start codes (e.g. jmuxer); the first frame already includes the parameter sets — no separate SPS/PPS request is needed.

## Appendix C. Version & Document Maintenance

- This document was generated from workspace `e42ea18` (2026-09-03); firmware version **4.3.1.61**.
- Version macros live in `Custom/Common/Inc/version.h` (auto-generated at build time); expected companion versions in `EXPECTED_FSBL_VERSION_STRING` and `EXPECTED_WIFI_VERSION_STRING`.
- Module routes are registered in `Custom/Services/Web/web_service.c`; route tables and error codes are in `web_server.h`, `api_business_error.h` and `api_isp_module.h`.
- After a firmware upgrade, re-check the API surface against `git diff` and update this document accordingly.



